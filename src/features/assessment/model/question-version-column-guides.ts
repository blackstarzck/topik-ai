/**
 * 문항 버전 컬럼 헤더 툴팁 문구 — 오해하기 쉬운 컬럼의 의미를 한 곳에서 관리한다.
 * 같은 컬럼이 문항 관리(확장 이력 표)와 인박스 목록 두 화면에 나오므로 문구를
 * 각 페이지에 복사하지 않고 여기서 단일 관리한다.
 */

// 공급처가 updated_at 을 실제로 갱신하지 않으면 생성 시각과 같은 값이 들어온다.
// 그래서 "두 시각이 같다 = 안 바뀌었다"로 읽으면 안 되고 content hash 로 판단해야 한다.
export const SOURCE_UPDATED_AT_COLUMN_GUIDE =
  '외부 공급처가 보낸 원본의 최종 수정 시각입니다. 공급처가 이 값을 실제로 갱신하지 않으면 원본 생성 시각과 같은 값이 그대로 들어오므로, 두 시각이 같다고 해서 내용이 바뀐 적 없다는 뜻은 아닙니다. 실제 변경 여부는 content hash 비교로 확인하세요.';

// 수신 횟수는 버전 수가 아니다. 동일 payload 재수신 시에만 증가하고,
// payload 가 달라지면 이 값이 아니라 새 버전 행(버전 ID)이 생긴다.
export const INGEST_COUNT_COLUMN_GUIDE =
  '같은 내용(동일 payload)을 외부 공급처에서 다시 받은 횟수입니다. 2회 이상이어도 버전이 늘어난 것이 아니라 같은 내용을 재수신해 마지막 수신 시각만 갱신된 것이며, 내용이 달라지면 이 값이 오르지 않고 새 버전 행이 따로 쌓입니다.';
