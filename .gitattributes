# Normalize text files in the repository and keep source files in UTF-8.
* text=auto eol=lf

# Windows command files keep CRLF for local execution ergonomics.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
