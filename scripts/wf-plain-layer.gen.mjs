export const meta = {
  name: 'api-plain-rewrite',
  description: 'Rewrite every catalogued API into plain, non-expert Korean (easy one-liners) for a more readable backend handoff doc',
  phases: [{ title: 'Rewrite', detail: 'one agent per domain rewrites its APIs in plain Korean' }],
}

const DATA = [{"domain":"assessment (문항/태그/쓰기)","project":"topik-ai","apis":[{"n":"admin_update_topik_question","op":"rpc","type":"supabase_rpc","p":"문항 노출 통제(service_status) 등 가변 컬럼을 화이트리스트 patch로 갱신하고 admin_audit_logs에 diff를 남기는 단일 쓰기 경로. assessment 도메인에서는 service_status 변경(노출/제외/내부테스트 전환)에만 사용한다(검수 축은 화이트리스트에 있으나 프론트는 미사용).","r":"void (RETURNS void) — 성공 시 반환값 없음. 변경분이 없으면(diff='{}') 감사 행도 남기지 않고 그냥 return. 프론트(setTopikWritingServiceStatus)는 반환을 쓰지 않고, 이어서 loadDetail로 상세를 재조회해 AssessmentQuestionDetail을 갱신함. 오류는 PostgREST error.message로 throw."},{"n":"admin_assign_question_tag","op":"rpc","type":"supabase_rpc","p":"문항에 운영 태그를 부여(이력 보존형). P4 관리 포인트 — 추천/반복방지/운영주의 등 tag_master 활성 사전에서 고른 태그를 부여하고 사유(memo)를 question_tags.memo에 기록.","r":"bigint (RETURNS bigint = 신규 tag_assignment_id). ⚠️프론트(assignTopikWritingQuestionTag)는 Promise<void>로 타입하고 반환값을 무시함 — 부여 후 useQuestionBankTags.reload()로 활성 태그 전수를 재조회. 오류는 error.message throw."},{"n":"admin_remove_question_tag","op":"rpc","type":"supabase_rpc","p":"문항 태그 제거(이력 보존형 — is_active=false + removed_at 갱신). 사유 필수.","r":"void (RETURNS void). 프론트(removeTopikWritingQuestionTag)는 반환 없음을 기대하고 이후 reload로 재조회. 오류는 error.message throw."},{"n":"admin_update_tag_master_status","op":"rpc","type":"supabase_rpc","p":"태그 마스터(tag_master) 사전의 활성/비활성 토글 — /system/metadata 마스터 카탈로그 화면(P5-3)에서 사용. 비활성화는 신규 부여 옵션 노출만 중단하며 기존 부여 행은 그대로 유지.","r":"void (RETURNS void). 프론트(setTopikWritingTagMasterStatus)는 반환 없음 기대 → 성공 후 카탈로그 재조회. 오류는 error.message throw."},{"n":"topik_writing_question_recommendation_view","op":"select","type":"supabase_table","p":"문항 목록(manage/bank) 화면의 단일 조회원 — 51~54 4개 테이블의 공통 컬럼을 UNION ALL한 읽기 전용 뷰. 목록 1회 조회로 AssessmentQuestionSummary[] 생성.","r":"ViewRow[] (PostgREST raw): { question_id: string; item_number: number; target_level: string|null; difficulty_level: number|null; topic_main: string; topic_detail: string; speech_act: string|null; scenario_type: string; recommendation_keys: "},{"n":"topik_writing_question_tags","op":"select","type":"supabase_table","p":"활성 태그 전수 조회 — 목록 화면 문항별 태그 수 표시 + 태그 편집 모달의 활성 태그 소스. (insert/update/delete는 직접 하지 않고 admin_assign/remove_question_tag RPC 단일 경로)","r":"raw rows: { tag_assignment_id: number(bigint); question_id: string; tag_code: string; tag_value: string|null; assigned_at: string(timestamptz); memo: string|null }[]. → 프론트 매핑 TopikWritingQuestionTagRow: { tagAssignmentId:number, questionId"},{"n":"topik_writing_tag_master","op":"select","type":"supabase_table","p":"태그 값 사전 조회. 두 가지 surface: (1) 태그 편집 옵션 축(활성만, 6컬럼) — POL-018 그룹 판정용, (2) /system/metadata 마스터 카탈로그 전수(비활성·전 그룹 포함, 9컬럼) — 토글 화면용. (상태 변경은 admin_update_tag_master_status RPC)","r":"옵션 축 → TopikWritingTagMasterRow: { tagCode:string, tagNameKo:string, tagGroup:string, description:string, usageRule:string('' 폴백), isActive:boolean }[]. 카탈로그 → TopikWritingTagMasterCatalogRow: { tagCode, tagNameKo, tagGroup, description, us"},{"n":"topik_writing_topic_master","op":"select","type":"supabase_table","p":"17개 고정 종합 주제 × 세부 내용 마스터 조회. 두 surface: (1) 문항 필터 주제 축(활성만, 3컬럼), (2) /system/metadata 마스터 카탈로그 전수(7컬럼, 비활성 포함).","r":"필터 축 → TopikWritingTopicMasterRow: { topicMain:string, topicDetail:string, sortOrder:number|null }[] → 훅에서 topicMain별 details 그룹핑(TopicAxisOption). 카탈로그 → TopikWritingTopicMasterCatalogRow: { topicId:number(bigint), topicMain:string, topicD"},{"n":"topik_writing_51_questions / _52_ / _53_ / _54_questions","op":"select","type":"supabase_table","p":"문항 상세(AssessmentQuestionDetail) 단건 조회. question_id 접두부(51/52/53/54)로 테이블을 라우팅해 .select('*').eq('question_id',...).maybeSingle(). 공통 컬럼 + 번호별 전용 content를 매핑.","r":"raw row 전 컬럼(공통 35+ : question_id, item_number, target_level, difficulty_level, topic_main/detail, secondary_topic_main/detail, text_type, speech_act, scenario_type, situation_summary, learning_goal_summary, prompt_text, resolved_text, mode"},{"n":"problems","op":"select","type":"supabase_table","p":"[봉인된 LEGACY 롤백 경로] v13 구 스키마 problems 테이블을 읽어 신규 AssessmentQuestionSummary/Detail 모델로 매핑. VITE_QUESTION_BANK_SOURCE=legacy 일 때만 활성(평시 비활성 — 기본은 topik_writing). P4 종료까지 봉인 보존.","r":"ProblemRow: { id:string; question_no:number|null; title:string|null; prompt:string|null; difficulty:number|null; topic_category_code:string|null; explanation:string|null; answer_key:unknown(jsonb); rubric:unknown(jsonb); created_at:string|n"}]},{"domain":"billing (환불 심사)","project":"topik-ai","apis":[{"n":"admin_approve_billing_refund","op":"rpc","type":"supabase_rpc","p":"환불 요청을 '승인(approved)' 상태로 전환하는 관리자 워크플로 RPC. SECURITY DEFINER로 commerce_refunds 행을 잠그고(for update) status='approved', processed_by=caller uid, processed_at=now(), review_reason=사유로 업데이트한 뒤 admin_audit_logs에 'refund_approved' 감사 로그를 남긴다. v13 payment_history로 환불 금액을 되돌려쓰지 않음(의도만 기록, payload.intent_only_v13_payment_history_pending=true).","r":"RPC 자체는 RETURNS void (반환값 없음). 성공 시 프론트(approveBillingRefundViaRpc)는 즉시 loadRefund(refundId)로 commerce_refunds를 재조회하여 RefundRow를 만들어 반환함. RefundRow = { id: string; paymentId: string; userId: string; userNickname: string; requestedAmount: nu"},{"n":"admin_reject_billing_refund","op":"rpc","type":"supabase_rpc","p":"환불 요청을 '거절(rejected)' 상태로 전환하는 관리자 워크플로 RPC. commerce_refunds 행을 잠그고 status='rejected', processed_by=caller uid, processed_at=now(), review_reason=사유로 업데이트한 뒤 admin_audit_logs에 'refund_rejected' 감사 로그를 남긴다. 승인과 달리 payment_history 의도 플래그는 없음.","r":"RPC 자체는 RETURNS void. 성공 시 프론트(rejectBillingRefundViaRpc)가 loadRefund(refundId)로 재조회하여 RefundRow 반환. RefundRow = { id; paymentId; userId; userNickname; requestedAmount:number; reason:string; status:'거절'(DB 'rejected' 매핑); requestedAt:'YYYY-"},{"n":"commerce_refunds","op":"select","type":"supabase_table","p":"환불 요청 워크플로 상태를 보관하는 admin-owned 테이블(SoT). 환불 목록 조회와 승인/거절 후 단건 재조회에 사용. 쓰기는 직접 INSERT/UPDATE가 아니라 위 두 SECURITY DEFINER RPC를 통해서만 일어남.","r":"select 후 CommerceRefundRow(raw) = { id:string; payment_id:string; user_id:string; user_nickname:string; requested_amount:number; reason:string; status:string('pending'|'approved'|'rejected'); requested_at:string(ISO timestamptz); processed_"},{"n":"payment_history","op":"select","type":"supabase_table","p":"결제 내역 조회(읽기 전용). v13 소유 테이블에 대한 느슨한 통합 — 환불 워크플로는 이 테이블을 쓰지 않고(환불 승인해도 되돌려쓰지 않음) 결제 목록 표시용으로만 조회. profiles/subscriptions를 임베드 조인해 회원명/상품명을 함께 가져온다.","r":"raw PaymentHistoryRow = { id; user_id; subscription_id:string|null; amount_cents:number; currency:string; status:string; paid_at:string|null; created_at:string; profiles?:{display_name:string|null; nickname:string|null}|null; subscriptions?"}]},{"domain":"commerce (쿠폰/포인트)","project":"topik-ai","apis":[{"n":"admin_save_commerce_coupon","op":"rpc","type":"supabase_rpc","p":"쿠폰 생성/수정(upsert). p_id가 null/빈문자열이면 생성(서버가 CPN-#### 신규 ID 채번), 값이 있으면 해당 쿠폰을 잠그고 수정. 저장 후 admin_audit_logs에 coupon_saved 기록. 반환은 쿠폰 ID 문자열뿐이라 프론트는 곧바로 commerce_coupons를 단건 select해서 전체 모델로 재매핑한다.","r":"RPC returns `text` = 저장된 쿠폰 id (예: \"CPN-0007\"). 프론트(saveCouponViaRpc)는 이 id로 commerce_coupons를 다시 select하여 CommerceCoupon 모델로 반환한다. CommerceCoupon = { id:string; couponName:string; couponKind:'customerDownload'|'autoIssue'|'couponCode'|'man"},{"n":"admin_duplicate_commerce_coupon","op":"rpc","type":"supabase_rpc","p":"기존 쿠폰을 복제. 원본을 잠그고 새 CPN-#### ID로 INSERT...SELECT. 복제본은 coupon_name+' 복사본', coupon_status='waiting', issue_state='normal', 사용/발급 카운트(issue_count/download_count/use_count)와 last_*_at은 0/null로 초기화. admin_audit_logs에 coupon_duplicated 기록.","r":"RPC returns `text` = 새로 생성된 복제 쿠폰 id. 프론트(duplicateCouponViaRpc)는 loadCoupon(id)로 재조회하여 CommerceCoupon 전체 모델 반환(shape는 admin_save_commerce_coupon 항목과 동일)."},{"n":"admin_set_commerce_coupon_issue_state","op":"rpc","type":"supabase_rpc","p":"쿠폰 발행 상태 토글(발행 중지/재개). p_state는 'paused' 또는 'normal'. autoIssue(자동 발행) 쿠폰만 허용 — coupon_kind<>'autoIssue'면 예외. 상태 전환 시 admin_audit_logs에 coupon_paused/coupon_resumed 기록.","r":"RPC returns `text` = p_coupon_id(변경된 쿠폰 ID 에코). 프론트(setCouponIssueStateViaRpc)는 loadCoupon(couponId)로 재조회 → CommerceCoupon 전체 모델 반환(shape 동일)."},{"n":"admin_delete_commerce_coupon","op":"rpc","type":"supabase_rpc","p":"쿠폰 영구 삭제(하드 딜리트). 대상 행을 잠그고 DELETE. admin_audit_logs에 coupon_deleted + 삭제 직전 전체 행 스냅샷(to_jsonb(v_old))을 diff로 기록.","r":"RPC returns `text` = p_coupon_id(삭제된 ID 에코). 프론트(deleteCouponViaRpc)는 RPC 호출 전에 loadCoupon으로 삭제 대상 스냅샷을 미리 받아두고, 삭제 성공 후 그 스냅샷(CommerceCoupon)을 반환한다(삭제 후엔 select 불가하므로)."},{"n":"admin_save_commerce_point_policy","op":"rpc","type":"supabase_rpc","p":"포인트 정책 생성/수정. p_id 없으면 신규(POL-#### 채번, status='draft' 고정 시작) / 있으면 수정. category 컬럼은 policy_type와 동일 값으로 자동 동기화. description도 condition_summary로 동기화 저장. admin_audit_logs에 point_policy_saved 기록.","r":"RPC returns `text` = 저장된 정책 id (POL-####). 프론트(savePointPolicyViaRpc)는 loadPointPolicy(id)로 commerce_point_policies 단건 select 후 PointPolicy 모델 반환. PointPolicy = { id:string; name:string; policyType:'적립'|'차감'|'소멸'; conditionSummary:string; e"},{"n":"admin_hold_commerce_point_expiration","op":"rpc","type":"supabase_rpc","p":"포인트 소멸 예정 건 보류(hold). status를 'held'로 전환(단, 이미 'completed'면 completed 유지), hold_reason/held_by/held_at 설정, calculation_memo에 '[시각 / 관리자] hold - 사유' 한 줄 append. admin_audit_logs에 point_expiration_held 기록.","r":"RPC returns `text` = p_expiration_id(에코). 프론트(savePointExpirationHoldViaRpc)는 loadPointExpiration(id)로 commerce_point_expirations 재조회 → PointExpiration 모델 반환. PointExpiration = { id:string; scheduledAt:string('YYYY-MM-DD HH:mm', expire_at 기"},{"n":"admin_release_commerce_point_expiration","op":"rpc","type":"supabase_rpc","p":"포인트 소멸 보류 해제. status를 무조건 'scheduled'(예정)로 되돌리고 hold_reason/held_by/held_at을 null로 초기화, calculation_memo에 '[시각 / 관리자] release - 사유' append. admin_audit_logs에 point_expiration_released 기록.","r":"RPC returns `text` = p_expiration_id(에코). 프론트(releasePointExpirationHoldViaRpc)는 loadPointExpiration(id) 재조회 → PointExpiration 모델 반환(shape는 admin_hold_commerce_point_expiration 항목과 동일, status는 '예정'으로 복귀)."},{"n":"admin_save_commerce_coupon_template","op":"rpc","type":"supabase_rpc","p":"(인접 RPC, 동일 파일/도메인) 정기 쿠폰 구독 템플릿 생성/수정(upsert). p_id 없으면 CPT-#### 채번 생성. issue_schedule/usage_end_schedule jsonb 객체와 등급/카테고리/제외상품 reference id 배열을 저장. admin_audit_logs에 coupon_template_saved 기록.","r":"RPC returns `text` = 저장된 템플릿 id (CPT-####). 프론트(saveCouponTemplateViaRpc)는 commerce_coupon_subscription_templates 단건 select 후 CommerceCouponSubscriptionTemplate 모델 반환: { id; templateName; issueTargetType:'shoppingGrade'; targetGradeIds:stri"},{"n":"admin_set_commerce_coupon_template_status","op":"rpc","type":"supabase_rpc","p":"(인접 RPC) 정기 쿠폰 템플릿 발행 상태 토글('active'|'paused'). admin_audit_logs에 coupon_template_paused/resumed 기록.","r":"RPC returns `text` = p_template_id 에코. 프론트는 loadTemplate(id) 재조회 → CommerceCouponSubscriptionTemplate 모델(shape는 admin_save_commerce_coupon_template 항목과 동일)."},{"n":"admin_delete_commerce_coupon_template","op":"rpc","type":"supabase_rpc","p":"(인접 RPC) 정기 쿠폰 템플릿 하드 딜리트. admin_audit_logs에 coupon_template_deleted + 삭제 직전 전체 행(to_jsonb) diff 기록.","r":"RPC returns `text` = p_template_id 에코. 프론트(deleteCouponTemplateViaRpc)는 삭제 전 loadTemplate으로 받은 스냅샷(CommerceCouponSubscriptionTemplate)을 삭제 성공 후 반환."},{"n":"admin_update_commerce_point_policy_status","op":"rpc","type":"supabase_rpc","p":"(인접 RPC) 포인트 정책 운영 상태 전환('active'|'inactive'). note 컬럼에 '[시각 / 관리자] status <상태> - 사유' append. admin_audit_logs에 point_policy_status_changed 기록.","r":"RPC returns `text` = p_policy_id 에코. 프론트(updatePointPolicyStatusViaRpc)는 loadPointPolicy(id) 재조회 → PointPolicy 모델(shape는 admin_save_commerce_point_policy 항목과 동일)."},{"n":"admin_create_manual_point_adjustment","op":"rpc","type":"supabase_rpc","p":"(인접 RPC) 회원 포인트 수동 적립/차감 조정. 대상 회원의 최신 원장 잔액을 잠그고 amount를 더해 새 잔액 계산 → 음수가 되면 'point balance cannot be negative' 예외(적자 미허용). entry_type은 amount<0이면 'debit', 아니면 'earn'. PL-#### 채번 후 commerce_point_ledgers INSERT. admin_audit_logs에 point_manual_adjusted 기록.","r":"RPC returns `text` = 생성된 원장 id (PL-####). 프론트(createManualPointAdjustmentViaRpc)는 loadPointLedger(id)로 commerce_point_ledgers 단건 select → PointLedger 모델 반환. PointLedger = { id:string; occurredAt:string('YYYY-MM-DD HH:mm'); userId:string; us"},{"n":"commerce_coupons","op":"select","type":"supabase_table","p":"쿠폰 마스터 테이블. 프론트는 PostgREST로 SELECT만 직접 수행(목록/단건). 모든 쓰기는 SECURITY DEFINER RPC 경유. RLS: authenticated 중 private.is_admin(auth.uid())인 관리자만 SELECT 가능.","r":"Row[] → mapCouponRow로 CommerceCoupon[]로 변환. 행 컬럼 타입: id text; coupon_name text; coupon_kind/coupon_status/issue_state text(ASCII enum); target_group_ids/target_group_names/target_user_ids/applicable_scope_reference_ids/excluded_product_ids/"},{"n":"commerce_coupon_subscription_templates","op":"select","type":"supabase_table","p":"정기(구독) 쿠폰 템플릿 마스터. 프론트는 SELECT만 직접 수행(목록/단건). 쓰기는 RPC 경유. RLS: 관리자(is_admin) SELECT만 허용.","r":"Row[] → mapTemplateRow로 CommerceCouponSubscriptionTemplate[]. 컬럼 타입: target_grade_ids/target_grade_names/applicable_scope_reference_ids/applicable_scope_reference_names/excluded_product_ids/excluded_product_names/policy_notes jsonb array; i"},{"n":"commerce_point_policies","op":"select","type":"supabase_table","p":"포인트 정책 마스터. 프론트는 스냅샷 로드 및 정책 저장 후 재조회 시 SELECT. 쓰기는 RPC 경유. RLS: 관리자 SELECT만.","r":"Row[] → mapPolicyRow로 PointPolicy[]. 컬럼: policy_type/category/status text(ASCII enum: earn/debit/expire, draft/active/inactive); amount/points integer(프론트 미사용); name/condition_summary/earn_debit_rule/expiration_rule/target_condition/trigger"},{"n":"commerce_point_ledgers","op":"select","type":"supabase_table","p":"포인트 원장(거래 내역). 프론트는 스냅샷 로드 및 수동 조정 후 단건 재조회 시 SELECT. 쓰기는 admin_create_manual_point_adjustment RPC 경유. RLS: 관리자 SELECT만.","r":"Row[] → mapLedgerRow로 PointLedger[]. 컬럼: entry_type text(earn/debit/revoke/restore/expire→UI 적립/차감/회수/복구/소멸); source_type text(referral/mission/event/payment/refund/admin/system→UI 추천/미션/이벤트/결제/환불/관리자/시스템); amount/balance_after/available_ba"},{"n":"commerce_point_expirations","op":"select","type":"supabase_table","p":"포인트 소멸 예정/이력. 프론트는 스냅샷 로드 및 보류/해제 후 단건 재조회 시 SELECT. 쓰기는 hold/release RPC 경유. RLS: 관리자 SELECT만.","r":"Row[] → mapExpirationRow로 PointExpiration[]. 컬럼: source_type text(7종 enum→UI 한글); scheduled_amount/available_amount integer(>=0 CHECK)→UI expiringPoint/availablePoint; expire_at timestamptz→scheduledAt('YYYY-MM-DD HH:mm'); status text(sched"}]},{"domain":"community (게시글/신고)","project":"topik-ai","apis":[{"n":"community_posts","op":"select","type":"supabase_table","p":"커뮤니티 게시글 목록/단건 조회. 관리자 게시글 관리 화면의 기본 읽기 경로. community_post_admin_notes(관리자 메모)를 임베디드로 함께 조회한다.","r":"Row: { id: string; title: string; content_html: string|null; author_id: string; author_name: string; board: string; status: string('published'|'hidden'); last_moderation_policy_code: string|null('SPAM'|'ABUSE'|'AD'|'PRIVACY'|'DUPLICATE'|'OT"},{"n":"community_reports","op":"select","type":"supabase_table","p":"커뮤니티 신고 목록/단건 조회. 신고 관리 화면의 기본 읽기 경로.","r":"Row: { id: string; target_post_id: string|null; target_user_id: string; target_user_name: string; reporter_id: string; reporter_name: string; reason: string; reason_code: string|null; process_status: string('pending'|'resolved'); resolution"},{"n":"admin_hide_community_post","op":"rpc","type":"supabase_rpc","p":"게시글을 '숨김(hidden)' 상태로 전환하고 마지막 조치 정책 코드를 기록. 감사 로그(post_hidden)를 남긴다.","r":"RETURNS text — 갱신된 p_post_id 문자열 그대로 반환. ⚠️프론트는 이 반환값을 사용하지 않고, 성공 시 loadCommunityPost(postId)로 community_posts 행을 재조회하여 CommunityPost를 반환한다(supabase-community-service.ts:259-277)."},{"n":"admin_show_community_post","op":"rpc","type":"supabase_rpc","p":"숨김 게시글을 '게시(published)' 상태로 복원하고 정책 코드를 갱신. 감사 로그(post_shown) 기록.","r":"RETURNS text — p_post_id 반환. ⚠️프론트 미사용, 성공 후 loadCommunityPost(postId) 재조회 결과(CommunityPost)를 반환(supabase-community-service.ts:259-277)."},{"n":"admin_delete_community_post","op":"rpc","type":"supabase_rpc","p":"게시글을 영구 삭제(하드 delete). 삭제 전 감사 로그(post_deleted)를 기록. 연결된 admin_notes는 FK on delete cascade로 함께 삭제됨.","r":"RETURNS text — 삭제된 p_post_id 반환. ⚠️프론트는 RPC 호출 '직전에' loadCommunityPost로 대상 행을 미리 읽어두고(target), 삭제 성공 후 그 사전 스냅샷(target)을 CommunityPost로 반환한다(행이 이미 삭제되어 재조회 불가, supabase-community-service.ts:299-317)."},{"n":"admin_add_community_post_memo","op":"rpc","type":"supabase_rpc","p":"게시글에 관리자 메모(community_post_admin_notes)를 추가. 메모 id를 서버에서 자동 생성하고 게시글 updated_at/by를 갱신, 감사 로그(post_memo_added)를 남긴다.","r":"RETURNS text — 새로 생성된 메모 id(v_note_id, '<postId>-MEMO-NN' 형식, next_community_post_admin_note_id로 생성). ⚠️프론트는 반환 id를 쓰지 않고 loadCommunityPost(postId)로 게시글을 재조회하여 갱신된 adminNotes 포함 CommunityPost를 반환(supabase-community-service.ts:319-342)."},{"n":"admin_resolve_community_report","op":"rpc","type":"supabase_rpc","p":"신고를 처리완료(resolved)로 전환하고 조치(action)를 기록. action='hide_post'이고 대상 게시글이 있으면 해당 게시글을 숨김 처리까지 수행. 감사 로그(report_resolved) 기록.","r":"RETURNS text — p_report_id 반환. ⚠️프론트 미사용, 성공 후 loadCommunityReport(reportId)로 community_reports 행을 재조회하여 CommunityReport를 반환(supabase-community-service.ts:344-361)."}]},{"domain":"message + auth-email (인증메일 템플릿)","project":"topik-ai","apis":[{"n":"auth_email_templates","op":"select","type":"supabase_table","p":"Supabase Auth 인증 메일 6종(가입인증/매직링크/비밀번호재설정/이메일변경/초대/재인증)의 편집본 + 동기화 상태를 읽는 테이블. 관리자 화면(/messages/mail 인증 메일 탭)이 목록/단건 조회에 사용한다. 쓰기는 PostgREST가 아니라 RPC 단일 경로로만 일어난다(테이블에 admin select RLS만 있고 insert/update/delete 정책 없음).","r":"Row[] (또는 maybeSingle 시 Row|null). DB 컬럼(select 한 것만): { id: uuid(string), auth_type: text, subject: text(default ''), body_html: text(default ''), status: text('draft'|'ready'|'published'|'archived'), sync_status: text('draft'|'synced'|'er"},{"n":"admin_save_auth_email_template","op":"rpc","type":"supabase_rpc","p":"인증 메일 템플릿 1종(auth_type 단위)을 upsert 저장하는 쓰기 RPC. 본문(subject/body_html)이 바뀌면 sync_status를 draft로 되돌려 '동기화 전까지 발송 미반영' 상태를 강제한다. admin_audit_logs에 사유와 함께 감사 기록을 남기는 쓰기 단일 경로.","r":"returns uuid — upsert된 행의 id 한 개(스칼라). ⚠️ 갱신된 행 전체를 반환하지 않음. 프론트(saveSupabaseAuthEmailTemplate)는 이 uuid를 무시하고 직후 loadOne(authType)으로 테이블을 다시 select 해서 AuthEmailTemplate을 만들어 반환한다. 따라서 화면이 받는 최종 shape는 auth_email_templates 행 매핑 결과와 동일."},{"n":"admin_mark_auth_email_synced","op":"rpc","type":"supabase_rpc","p":"서버 /api/auth-email/sync가 Management API로 실제 PATCH+GET 검증을 끝낸 뒤, 그 결과(성공/실패)를 DB에 감사 기록하는 RPC. 성공 시 sync_status를 synced(또는 live가 편집본과 다르면 drift)로, 실패 시 error로 기록한다. 성공/실패 모두 호출되는 단일 감사 경로.","r":"returns uuid — 갱신된 행의 id 한 개(스칼라). ⚠️ 행 전체 미반환. 프론트는 RPC 성공 후(실패 시 throw) loadOne(authType)으로 재조회하여 AuthEmailTemplate을 반환한다."},{"n":"POST /api/auth-email/sync","op":"POST","type":"next_api_route","p":"관리자가 편집한 인증 메일 템플릿(auth_email_templates의 DB 편집본)을 Supabase Auth 내장 템플릿에 실제로 push 동기화하는 서버 엔드포인트. Management API 토큰/Service Role 키는 서버 전용이므로 브라우저는 자신의 access_token만 Bearer로 전달한다. 흐름: 관리자 JWT 검증 → profiles.app_role 게이트 → DB 템플릿 읽기 → Management API GET(롤백 스냅샷) → PATCH(mailer_subjects/templates_content) → GET 재검증(live==푸시값). 실제 DB 기록은 호출 측이 admin_mark_auth_email_synced로 수행.","r":"200 OK: { ok: boolean, snapshot: { mailer_subjects_<authType>: <prev>|null, mailer_templates_<authType>_content: <prev>|null }|null, error?: string }. ok=true는 PATCH 후 GET 재검증에서 live subject/body가 푸시값과 정확히 일치할 때만. live 불일치 시 200이지만 ok:false"}]},{"domain":"operation (공지/FAQ/약관/이벤트)","project":"topik-ai","apis":[{"n":"operation_notices","op":"select","type":"supabase_table","p":"공지사항 SoT 테이블. 목록/단건 읽기 전용(쓰기는 RPC 단일 경로). status는 published/hidden ASCII 저장, UI는 게시/숨김으로 매핑.","r":"OperationNoticeRow { id: string; title: string; body_html: string; status: 'published'|'hidden'; author: string; created_at: string|null (timestamptz ISO); updated_at: string|null; updated_by: string|null } — 프론트는 mapNoticeRow로 OperationNot"},{"n":"admin_save_operation_notice","op":"rpc","type":"supabase_rpc","p":"공지 생성/수정 단일 write 경로. p_id 없으면 신규(NOTICE-NNN 자동 채번, status='hidden'), 있으면 수정. 감사 로그(notice_saved) 적재.","r":"text — 저장된 공지 id (예: 'NOTICE-003'). 프론트는 이 id로 loadOperationNotice 재조회하여 OperationNotice를 화면에 반영."},{"n":"admin_toggle_operation_notice_status","op":"rpc","type":"supabase_rpc","p":"공지 노출 상태 전환(게시↔숨김). 동일 상태로의 전환은 거부. 감사 로그(notice_status_changed).","r":"text — 대상 notice id. 프론트는 반환 무시하고 loadOperationNotice로 재조회한 OperationNotice를 반환."},{"n":"admin_delete_operation_notice","op":"rpc","type":"supabase_rpc","p":"공지 영구 삭제(hard delete). 삭제 전 스냅샷을 감사 payload(notice_deleted)에 기록.","r":"text — 삭제된 notice id. 프론트는 삭제 직전 loadOperationNotice로 받아둔 OperationNotice 스냅샷을 반환(삭제 후 재조회 아님)."},{"n":"operation_faqs","op":"select","type":"supabase_table","p":"FAQ 원문 SoT. 목록/단건 읽기. category는 한글 enum(계정/결제/커뮤니티/메시지), status는 published/hidden.","r":"OperationFaqRow { id: string; question: string; answer: string; search_keywords: jsonb(string[]); category: '계정'|'결제'|'커뮤니티'|'메시지'; status: 'published'|'hidden'; created_at/updated_at: string|null; updated_by: string|null } → OperationFaq{ "},{"n":"operation_faq_curations","op":"select","type":"supabase_table","p":"FAQ 대표 노출(큐레이션) 규칙 SoT. surface별 표시 순위/노출 상태/고정 기간 관리. surface+display_rank 전역 유니크.","r":"OperationFaqCurationRow { id: string; faq_id: string; surface: 'help_center'|'home_top'|'payment_help'|'onboarding'; curation_mode: 'manual'|'auto'; display_rank: number(smallint>0); exposure_status: 'active'|'paused'; pinned_start_at: date"},{"n":"operation_faq_metrics","op":"select","type":"supabase_table","p":"FAQ 조회/검색/도움 카운트 read-model. 관리자 write RPC 없음(시드+읽기 전용 표시용).","r":"OperationFaqMetricRow { faq_id: string; view_count: number; search_hit_count: number; helpful_count: number; not_helpful_count: number; last_viewed_at: string|null } → OperationFaqMetric{ faqId,viewCount,searchHitCount,helpfulCount,notHelpf"},{"n":"admin_save_operation_faq","op":"rpc","type":"supabase_rpc","p":"FAQ 원문 생성/수정. 신규는 FAQ-NNN 채번+status='hidden'. 수정 시 status='hidden'으로 바꾸면 연결 큐레이션을 paused로 강제. 감사(faq_saved).","r":"text — 저장된 FAQ id (예: 'FAQ-004'). 프론트는 이 id로 loadOperationFaq 재조회."},{"n":"admin_toggle_operation_faq_status","op":"rpc","type":"supabase_rpc","p":"FAQ 공개/비공개 전환. hidden 전환 시 active 큐레이션을 paused로 일괄 변경하고 paused된 id 목록을 감사 payload(faq_status_changed)에 기록.","r":"text — FAQ id. 프론트는 loadOperationFaq로 재조회한 OperationFaq 반환."},{"n":"admin_delete_operation_faq","op":"rpc","type":"supabase_rpc","p":"FAQ 영구 삭제(연결 큐레이션/메트릭은 FK cascade). 삭제 전 큐레이션/메트릭 개수와 스냅샷을 감사(faq_deleted)에 기록.","r":"text — 삭제된 FAQ id. 프론트는 삭제 직전 읽어둔 OperationFaq 스냅샷을 반환."},{"n":"admin_save_operation_faq_curation","op":"rpc","type":"supabase_rpc","p":"FAQ 대표 노출 규칙 생성/수정. 신규는 FAQCUR-NNN 채번. surface+display_rank 중복 거부, 비공개 FAQ는 active 큐레이션 불가. 감사(faq_curation_saved).","r":"text — 저장된 큐레이션 id (예: 'FAQCUR-004'). 프론트는 loadOperationFaqCuration 재조회."},{"n":"admin_delete_operation_faq_curation","op":"rpc","type":"supabase_rpc","p":"FAQ 대표 노출 규칙 삭제. 삭제 전 surface/display_rank/exposure 스냅샷을 감사(faq_curation_deleted)에 기록.","r":"text — 삭제된 curation id. 프론트는 삭제 직전 읽어둔 OperationFaqCuration 스냅샷 반환."},{"n":"operation_policies","op":"select","type":"supabase_table","p":"정책/약관 SoT(법률/약관, 결제/리워드 등 16종). 다국어(ko 본문 + title_en/body_html_en/summary_en). 읽기 전용, 쓰기는 RPC.","r":"OperationPolicyRow { id; category; policy_type; title; version_label:string|null; effective_date:date|null; exposure_surfaces:jsonb(string[]); requires_consent:boolean|null; tracking_status:string|null('코드 반영'|'문서 추적'|'정책 미확정'); status:'pub"},{"n":"operation_policy_histories","op":"select","type":"supabase_table","p":"정책 버전 이력. action별 스냅샷(camelCase OperationPolicy) 보관. 버전 복원(publish version)의 소스.","r":"OperationPolicyHistoryRow { id:string; policy_id:string; action:'created'|'updated'|'status_changed'|'version_published'|'deleted'; version_label:string|null; changed_at:string|null; changed_by:string|null; snapshot:jsonb(operation_policy_s"},{"n":"admin_save_operation_policy","op":"rpc","type":"supabase_rpc","p":"정책 생성/수정. 신규는 POL-NNN 채번+status='hidden'. mode='version'이면 수정 시에도 status='hidden'으로 재설정(새 버전 초안). 매 저장마다 history(PH-NNNN) append + current_version_id 갱신. 감사(policy_saved).","r":"text — 저장된 정책 id (예: 'POL-017'). 프론트는 loadOperationPolicy 재조회. 단 p_policy에는 영문(title_en 등) 미포함 → 영문 컬럼은 이 RPC로 쓰이지 않음(현재 SQL에 영문 update 없음)."},{"n":"admin_toggle_operation_policy_status","op":"rpc","type":"supabase_rpc","p":"정책 게시/숨김 전환. status_changed history append. 게시(이용약관/개인정보 처리방침)면 프론트가 후속으로 legal_documents 동기화 RPC 호출. 감사(policy_status_changed).","r":"text — policy id. 프론트는 loadOperationPolicy 재조회 후, 약관/개인정보+게시 상태면 admin_sync_legal_document_from_operation_policy를 추가 호출."},{"n":"admin_delete_operation_policy","op":"rpc","type":"supabase_rpc","p":"정책 삭제. 삭제 전 'deleted' history append(스냅샷 보존) + 감사(policy_deleted, payload에 snapshot 포함) 후 hard delete.","r":"text — 삭제된 policy id. 프론트는 삭제 직전 읽어둔 OperationPolicy 스냅샷 반환."},{"n":"admin_publish_operation_policy_version","op":"rpc","type":"supabase_rpc","p":"특정 history 스냅샷을 정책 본문으로 복원하며 status='published'로 발행. version_published history append. 발행 후 프론트가 legal 동기화 호출. 감사(policy_version_published).","r":"text — policy id. 프론트는 loadOperationPolicy 재조회 + syncLegalProjection 호출."},{"n":"admin_sync_legal_document_from_operation_policy","op":"rpc","type":"supabase_rpc","p":"(v13 소유 RPC) operation_policies(SoT) 게시 본문을 사용자측 legal_documents 미러에 투영. 이용약관→terms, 개인정보 처리방침→privacy. ko 항상 + en은 영문 title+body가 있을 때만. 같은 (doc_type,version,locale) 다른 본문 재발행은 거부(불변).","r":"jsonb { doc_type: 'terms'|'privacy'; version: string; written: uuid[] (생성/갱신된 legal_documents.id 배열) }. 프론트(syncLegalProjection)는 반환값을 사용하지 않고 에러만 처리."},{"n":"admin_send_terms_change_notification","op":"rpc","type":"supabase_rpc","p":"이용약관 버전 변경 알림(인앱+이메일)을 전체 활성 사용자에게 수동 발송. 활성 회원 스냅샷을 '전체 활성 사용자' 정적 그룹에 적재 후 in_app/email 디스패치 2건 생성. 실제 집행은 v13 pg_cron 디스패처.","r":"jsonb { group_id: uuid; recipients: number(int); in_app_dispatch: uuid; email_dispatch: uuid } → 프론트 TermsChangeNotificationResult{ recipients:number, inAppDispatch:string|null(=in_app_dispatch), emailDispatch:string|null(=email_dispatch) }"},{"n":"operation_events","op":"select","type":"supabase_table","p":"운영 이벤트 SoT(프로모션/출석/챌린지/리워드). 노출/진행 상태, 기간, 배너, 보상/메시지템플릿(denormalized), SEO 메타 보관. 읽기 전용.","r":"OperationEventRow { id; title; summary:string|null; body_html; slug:string|null; event_type:'프로모션'|'출석'|'챌린지'|'리워드'; visibility_status:'exposed'|'hidden'|'scheduled'; progress_status:'ongoing'|'upcoming'|'ended'; start_at/end_at:date|null; "},{"n":"admin_save_operation_event","op":"rpc","type":"supabase_rpc","p":"이벤트 생성/수정. 신규는 EVT-NNN 채번+progress_status='upcoming', participant_count=0 강제. 감사(event_saved). visibility는 페이로드 값 사용(상태전이 RPC와 별개).","r":"text — 저장된 이벤트 id (예: 'EVT-004'). 프론트는 loadOperationEvent 재조회."},{"n":"admin_schedule_operation_event","op":"rpc","type":"supabase_rpc","p":"이벤트 게시 예약: visibility_status='scheduled'로 전환. 감사(event_scheduled).","r":"text — event id. 프론트는 loadOperationEvent 재조회한 OperationEvent 반환."},{"n":"admin_publish_operation_event","op":"rpc","type":"supabase_rpc","p":"이벤트 즉시 게시: visibility_status='exposed'로 전환. 감사(event_published).","r":"text — event id. 프론트는 loadOperationEvent 재조회."},{"n":"admin_end_operation_event","op":"rpc","type":"supabase_rpc","p":"이벤트 종료: progress_status='ended' + visibility_status='hidden' 동시 전환. 감사(event_ended).","r":"text — event id. 프론트는 loadOperationEvent 재조회한 OperationEvent 반환(progressStatus는 ended로 고정 표시)."}]},{"domain":"system (감사로그/관리자/메타데이터/권한)","project":"topik-ai","apis":[{"n":"admin_list_audit_logs","op":"rpc","type":"supabase_rpc","p":"감사 로그(admin_audit_logs) 읽기 전용 조회. /system/audit-logs 화면이 단일 소스로 사용. 필터(대상유형/대상ID/키워드/기간)·페이지네이션·총건수를 함께 반환. diff/payload 민감정보는 platform_admin에게만 노출.","r":"RETURNS TABLE → 행 배열(rows). 각 행: { log_id: text; target_type: text; target_id: text; action: text; actor: text; reason: text|null; diff: jsonb|null; payload: jsonb|null; created_at: timestamptz(ISO 문자열); total_count: bigint }. created_at de"},{"n":"admin_list_admins","op":"rpc","type":"supabase_rpc","p":"관리자 계정 디렉터리(읽기 전용). /system/admins 화면 목록. v13 소유 public.profiles + auth.users 조인하여 app_role <> 'learner'인 계정 반환.","r":"RETURNS TABLE → 행 배열. 각 행: { user_id: uuid; email: text|null; display_name: text|null; nickname: text|null; app_role: text|null; status: text|null; last_sign_in_at: timestamptz|null; created_at: timestamptz|null; updated_at: timestamptz|nul"},{"n":"admin_list_admin_app_roles","op":"rpc","type":"supabase_rpc","p":"권한 변경 화면(/system/permissions) 전용 관리자 계정 목록. 등급 변경 대상 staff 모집단(app_role<>learner)을 SQL에서 필터해 learner 페이지 잠식 방지. platform_admin 전용 조회.","r":"RETURNS TABLE → 행 배열. 각 행: { user_id: uuid; email: text|null; display_name: text|null; nickname: text|null; app_role: text; status: text; last_sign_in_at: timestamptz|null; created_at: timestamptz }. 프론트 매핑(mapRpcRow) → AdminAppRoleRow: { a"},{"n":"admin_set_admin_app_role","op":"rpc","type":"supabase_rpc","p":"관리자 계정의 app_role(권한 등급) 변경. /system/permissions 화면의 실 등급 변경 액션. profiles.app_role(RBAC SoT)에 쓰고 admin_audit_logs에 감사 기록. platform_admin 전용, 사유 필수.","r":"RETURNS uuid → 변경된 대상 user_id(p_target_user_id). 프론트(changeAdminAppRole)는 반환값을 사용하지 않고 error만 확인(Promise<void>). 성공 시 화면이 목록 재조회."},{"n":"admin_save_metadata_group","op":"rpc","type":"supabase_rpc","p":"메타데이터 그룹 생성/수정(upsert). /system/metadata 화면. p_group_id null이면 신규(서버에서 META-GRP-NNN 생성), 있으면 수정. 감사 기록 동반.","r":"RETURNS text → 저장된 그룹 id(group_id). 프론트(saveMetadataGroupViaRpc)는 이 id로 loadGroup(String(data)) 재조회하여 SystemMetadataGroup 전체를 반환: { groupId; groupName; description; managerType; ownerModule; ownerRole; status; syncStatus; exposureStatus; li"},{"n":"admin_save_metadata_item","op":"rpc","type":"supabase_rpc","p":"메타데이터 그룹 항목(코드/옵션) 생성/수정(upsert). p_item_id null이면 신규 채번, 있으면 수정. is_default=true면 그룹 내 타 항목 default 해제. 감사 기록 동반.","r":"RETURNS text → 항목이 속한 group_id. 프론트는 loadGroup(group_id) 재조회로 SystemMetadataGroup 전체 반환(items 포함). 단일 SystemMetadataItem: { itemId; code; label; description; status('active'|'inactive'); sortOrder: number; isDefault: boolean; exposureStatus"},{"n":"admin_delete_metadata_item","op":"rpc","type":"supabase_rpc","p":"메타데이터 항목 삭제. 삭제 항목이 default였으면 잔여 항목 첫번째를 default로 승계, sort_order 재정렬. 감사 기록 동반.","r":"RETURNS text → 항목이 속했던 group_id. 프론트는 loadGroup으로 SystemMetadataGroup 전체(잔여 items, 재정렬된 sortOrder) 재조회."},{"n":"admin_reorder_metadata_items","op":"rpc","type":"supabase_rpc","p":"메타데이터 그룹 내 항목 순서 재배치. 전달된 id 순서대로 sort_order(1..N) 재부여. 감사 기록 동반.","r":"RETURNS text → group_id. 프론트는 loadGroup으로 SystemMetadataGroup 전체(재정렬된 items) 재조회."},{"n":"admin_toggle_metadata_group_status","op":"rpc","type":"supabase_rpc","p":"메타데이터 그룹 활성/비활성(status) 토글. 감사 기록 동반.","r":"RETURNS text → group_id. 프론트는 loadGroup으로 SystemMetadataGroup 전체(갱신된 status) 재조회."},{"n":"admin_toggle_metadata_item_status","op":"rpc","type":"supabase_rpc","p":"메타데이터 항목 활성/비활성(status) 토글. 항목 소속 그룹 updated_at 갱신, 감사 기록 동반.","r":"RETURNS text → 항목이 속한 group_id. 프론트는 loadGroup으로 SystemMetadataGroup 전체(갱신된 item status) 재조회."},{"n":"system_logs","op":"select","type":"supabase_table","p":"시스템 로그(INFO/WARN/ERROR) 읽기 전용 조회. /system/logs 화면. PostgREST select로 created_at 내림차순 전체 조회.","r":"SystemLogDbRow[] → 프론트 매핑(mapSystemLogRow) → SystemLogRow[]: { id: string; level: 'INFO'|'WARN'|'ERROR'(WARN/ERROR 외엔 INFO로 정규화); component: string; message: string; traceId?: string(trace_id null→undefined); context?: unknown(jsonb 그대로); c"},{"n":"system_metadata_groups","op":"select","type":"supabase_table","p":"메타데이터 그룹 목록 읽기. /system/metadata 화면 로드 시 PostgREST select(updated_at desc). 쓰기는 admin_save/toggle RPC를 통해서만.","r":"MetadataGroupRow[] → 프론트 매핑(mapGroupRow, items와 결합) → SystemMetadataGroup[]: { groupId; groupName; description; managerType; ownerModule; ownerRole; status('active'|'inactive'); syncStatus('live'|'review'|'draft'); exposureStatus('confirmed"},{"n":"system_metadata_group_items","op":"select","type":"supabase_table","p":"메타데이터 그룹 항목(코드/옵션) 목록 읽기. 그룹 select와 병렬로 sort_order asc 조회하여 그룹별로 결합. 쓰기는 admin_*_metadata_item RPC만.","r":"MetadataItemRow[] → 프론트 매핑(mapItemRow) → SystemMetadataItem: { itemId; code; label; description; status('active'|'inactive'); sortOrder: number; isDefault: boolean; exposureStatus('confirmed'|'inferred'|'internalOnly'|'planned'); updatedAt("}]},{"domain":"users (회원/강사/추천인/기관코드)","project":"topik-ai","apis":[{"n":"get_admin_users","op":"rpc","type":"supabase_rpc","p":"회원 목록 read. profiles + auth.users 조인에 더해 writing_submissions 제출 집계, social_providers(auth.identities, email 제외), 약관 동의(legal_documents⋈user_consents) 집계, 박람회/기관 유입(profiles.affiliation_code ⋈ institution_codes.label)을 한 행으로 반환. 회원 목록 화면과 회원 상세 헤더(목록에서 find)에 사용.","r":"set of rows (RETURNS TABLE). 각 행: { user_id: uuid; email: text|null; display_name: text|null; nickname: text|null; app_role: text; plan_label: text|null; status: text; nationality_country_code: text|null; social_providers: text[]; affiliati"},{"n":"get_admin_user_learning_overview","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 학습 현황 탭. v13 학습 테이블(problem_attempts/problems/writing_submissions/writing_feedback/feedback_dimension_scores/learning_goals) 읽기 집계. KPI/도메인 정확도/취약점/최근 풀이/최근 작문을 jsonb 5컬럼으로 반환. 작문 답안 본문·문장 피드백 텍스트(PII)는 제외.","r":"단일 행(RETURNS TABLE, 프론트는 Array면 [0] 취함). { kpis: jsonb; domain_accuracy: jsonb; weaknesses: jsonb; recent_attempts: jsonb; recent_writing: jsonb }. kpis={ totalAttempts:int; solvedProblems:int; correctRate:number|null; averageScore:number|n"},{"n":"admin_set_user_status","op":"rpc","type":"supabase_rpc","p":"회원 정지(blocked)/해제(active) write. active|blocked만 허용(deleted 차단). admin_audit_logs에 user_status_changed 기록.","r":"uuid — 변경된 target_id를 그대로 반환. 프론트는 반환값 미사용(에러만 체크)."},{"n":"admin_list_user_memos","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 관리자 메모 탭 목록. admin-owned public.user_admin_memos를 user_id로 조회(created_at desc).","r":"set of rows: { id: text; admin_name: text; content: text; created_at: timestamptz }. 프론트 매핑 → UserAdminMemo { id; admin(=admin_name); content; createdAt('YYYY-MM-DD' slice) }"},{"n":"admin_add_user_memo","op":"rpc","type":"supabase_rpc","p":"회원 상세 메모 추가(write). user_admin_memos insert + admin_audit_logs(user_memo_added) 기록.","r":"text — 생성된 메모 id('UMEMO-...'). 프론트는 String(data)로 반환."},{"n":"admin_delete_user_memo","op":"rpc","type":"supabase_rpc","p":"회원 상세 메모 삭제(write). user_admin_memos delete + admin_audit_logs(user_memo_deleted) 기록.","r":"text — 삭제된 메모 id를 그대로 반환. 프론트는 String(data)."},{"n":"admin_get_user_community_posts","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 커뮤니티 탭. admin-owned public.community_posts를 author_id로 read-only 조회(created_at desc).","r":"set of rows: { id: text; title: text; board: text; status: text; reports_count: int; created_at: timestamptz }. 프론트 매핑 → UserCommunityPost { id; title; board; createdAt('YYYY-MM-DD'); reports(=reports_count); status(published→게시, hidden→숨김,"},{"n":"admin_get_user_activity","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 활동 탭. admin-owned public.user_activity_events를 user_id로 read-only 조회(created_at desc). 표시 전용(쓰기/감사 없음).","r":"set of rows: { id: text; event_type: text; content: text; ip: text; created_at: text }. created_at은 KST 텍스트('YYYY-MM-DD HH24:MI'). 프론트 매핑 → UserActivityEvent { id; type(=event_type); content; createdAt(=created_at 그대로); ip }"},{"n":"admin_get_user_payments","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 결제 탭. admin-owned public.user_payment_records를 user_id로 read-only 조회(paid_at desc). 표시 전용.","r":"set of rows: { id: text; product: text; amount_krw: int; method: text; status: text; paid_at: text|null }. paid_at은 'YYYY-MM-DD' 텍스트(date를 to_char). 프론트 매핑 → UserPaymentRecord { id; product; amount(='₩'+amount_krw.toLocaleString()); method;"},{"n":"admin_get_user_access_logs","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 접속 로그 탭. admin-owned public.user_access_logs를 user_id로 read-only 조회(created_at desc). 표시 전용.","r":"set of rows: { id: text; log_type: text; ip: text; device: text; created_at: text }. created_at은 KST 텍스트('YYYY-MM-DD HH24:MI'). 프론트 매핑 → UserAccessLog { id; type(=log_type); ip; device; createdAt(=created_at) }"},{"n":"admin_get_user_legal_consents","op":"rpc","type":"supabase_rpc","p":"회원 상세 > 약관 동의 탭. v13 user_consents⋈legal_documents를 read-only 참조해 회원이 동의한 doc_type(이용약관/개인정보)별 최신 동의 1건 + 현재 게시 버전 일치 여부(is_current)를 반환.","r":"set of rows: { doc_type: text; version: text; title: text; source: text; accepted_at: text(KST 'YYYY-MM-DD HH24:MI'); is_current: boolean }. 프론트 매핑 → UserLegalConsent { docType; docLabel(terms→이용약관/privacy→개인정보 처리방침/그외 원본); version; title; "},{"n":"admin_list_instructors","op":"rpc","type":"supabase_rpc","p":"강사 관리 목록. admin-owned public.instructors 전체 상세(중첩 jsonb + 강사 메모 포함) 반환 — 목록/상세 드로어를 데이터소스 교체만으로 동작시키기 위해 list가 full detail을 반환.","r":"set of rows(InstructorRow): { id:text; real_name:text; email:text; nickname:text; organization:text; country:text; status:text; activity_status:text; assignment_status:text; course_count:int|null; student_count:int|null; last_activity_at:te"},{"n":"admin_get_instructor","op":"rpc","type":"supabase_rpc","p":"강사 상세 단건 조회. instructors + instructor_admin_notes 집계 1행 반환. 상태 변경(set_instructor_status) 후 재조회에 사용.","r":"set of rows(0 or 1) — admin_list_instructors와 동일한 full detail 컬럼셋: { id; real_name; email; nickname; organization; country; status; activity_status; assignment_status; course_count:int; student_count:int; last_activity_at:text; last_action_"},{"n":"admin_set_instructor_status","op":"rpc","type":"supabase_rpc","p":"강사 정지/해제/탈퇴 처리(write). instructors.status 갱신 + admin_audit_logs(instructor_status_changed) 기록. 프론트는 성공 후 admin_get_instructor로 재조회.","r":"text — 변경된 p_instructor_id 반환. 단, 프론트 setInstructorStatusViaRpc는 이 반환을 무시하고 admin_get_instructor 결과(InstructorDetail|null)를 반환."},{"n":"admin_list_referrals","op":"rpc","type":"supabase_rpc","p":"추천인 관리 목록. admin-owned referrals + 중첩 relations/reward_ledger + 읽기 시 파생 집계(추천/확정 수, 총 보상, 이상치 플래그, last_used_at)를 반환. 보상 정책은 미확정(STUB).","r":"set of rows(ReferralRow): { id:text; code:text; referrer_user_id:text|null; referrer_name:text|null; referrer_email:text|null; created_at:text|null; expires_at:text|null; last_used_at:text(파생, relations 최대 confirmed_at/joined_at); last_acti"},{"n":"admin_set_referral_status","op":"rpc","type":"supabase_rpc","p":"추천 코드 활성/비활성(write). referrals.status + last_action_at 갱신 + admin_audit_logs(referral_status_changed) 기록.","r":"text — p_referral_id 반환. 프론트 setReferralStatusViaRpc는 반환 무시(void), 페이지가 재조회."},{"n":"admin_review_referral_anomaly","op":"rpc","type":"supabase_rpc","p":"추천 이상치 검토 완료 처리(write). anomaly_status='검토 완료'로 변경 + admin_memo에 검토 내역 append + admin_audit_logs(referral_anomaly_reviewed) 기록.","r":"text — p_referral_id 반환. 프론트는 void 취급, 재조회로 갱신."},{"n":"admin_adjust_referral_reward","op":"rpc","type":"supabase_rpc","p":"추천 보상 수동 보정(write, POLICY STUB). referral_reward_ledgers에 감사 원장 1건 insert만 — 확정/회수 자동화 없음. amount 부호로 entry_type 결정(>=0 '수동 보정', <0 '회수'). admin_audit_logs(referral_reward_adjusted) 기록.","r":"text — 생성된 ledger id('ADJ-<uuid hex>'). 프론트는 String(data)로 반환."},{"n":"admin_list_institution_codes","op":"rpc","type":"supabase_rpc","p":"기관/박람회 코드 카탈로그 목록. admin-owned public.institution_codes + member_count(profiles.affiliation_code 집계) 반환.","r":"set of rows(InstitutionCodeRow): { code:text; label:text; kind:text; status:text; note:text|null; member_count:bigint|null; created_at:timestamptz|null; updated_at:timestamptz|null }. 프론트 매핑 → InstitutionCode { code; label; kind('박람회'|'기관'|"},{"n":"admin_create_institution_code","op":"rpc","type":"supabase_rpc","p":"기관/박람회 코드 생성(write). institution_codes insert + admin_audit_logs(institution_code_created) 기록.","r":"text — 생성된 code 반환. 프론트는 data ?? 입력 code."},{"n":"admin_update_institution_code","op":"rpc","type":"supabase_rpc","p":"기관/박람회 코드 수정(write). label/kind/status/note 갱신 + admin_audit_logs(institution_code_updated) 기록. code(PK)는 변경 불가.","r":"text — p_code 반환. 프론트는 data ?? input.code."},{"n":"profiles","op":"select","type":"supabase_table","p":"회원 닉네임 보강용 직접 select. get_admin_users 결과에서 nickname이 NULL/빈값인 행을 채우기 위해 id 배열로 profiles.id,nickname을 조회(loadProfileNicknameMap).","r":"{ id: string; nickname: string|null }[] — 프론트는 Map<id, nickname(trim 후 빈값은 null)>으로 변환해 AdminUserRow.nickname 보강."}]},{"domain":"notifications (알림 발송/템플릿/그룹)","project":"topik-ai","apis":[{"n":"admin_send_notification","op":"rpc","type":"supabase_rpc","p":"활성 템플릿의 발송 실행(dispatch)을 생성한다. 즉시 발송(scheduled_at=null → status=running)과 예약 발송(scheduled_at 지정 → status=scheduled), '나에게 보내기' 테스트(target_type=test, 그룹 없이 본인 대상)를 모두 처리. 실제 대상 산정·전달은 별도 발송 파이프라인이 수행하고, 이 RPC는 ledger 행 1개만 생성한다.","r":"uuid — 생성된 notification_dispatches.id 단일 스칼라. 프런트 어댑터(sendNotification)는 String(data)로 받아 { id: string }로 래핑해 반환."},{"n":"admin_cancel_notification_dispatch","op":"rpc","type":"supabase_rpc","p":"예약(scheduled) 상태의 발송 실행을 취소한다(QA N-ADM-11). status를 'canceled'로 전이하면 발송 파이프라인이 더 이상 집행하지 않아 발송 0건. 이미 실행/완료된 건은 취소 불가.","r":"void (returns void) — RPC 자체는 값을 반환하지 않음. 어댑터(cancelNotificationDispatch)는 입력 dispatchId를 그대로 { id: dispatchId }로 반환."},{"n":"admin_save_notification_template","op":"rpc","type":"supabase_rpc","p":"알림 템플릿 insert/update 겸용 저장(upsert). p_id=null이면 신규, 있으면 갱신. 쓰기 단일 경로(직접 INSERT/UPDATE는 RLS 차단). link_url 및 이메일 본문 100KB 가드 포함.","r":"uuid — 저장된 notification_templates.id 단일 스칼라. 어댑터(saveNotificationTemplate)는 이 id로 loadNotificationTemplate를 재호출해 매핑된 MessageTemplate { id, channel('mail'|'push'|'in_app'), mode, category, name, summary, subject, targetGroupIds:string[], sta"},{"n":"admin_delete_notification_template","op":"rpc","type":"supabase_rpc","p":"알림 템플릿 삭제. 어댑터는 삭제 직전에 행을 한 번 읽어 두고 삭제 후 그 스냅샷을 반환한다(UI에서 삭제된 대상 정보 표시용).","r":"void (returns void). 어댑터(deleteNotificationTemplate)는 삭제 전 loadNotificationTemplate로 읽어둔 MessageTemplate | null을 반환(삭제된 행의 스냅샷). RPC 자체는 값 반환 없음."},{"n":"admin_set_notification_template_status","op":"rpc","type":"supabase_rpc","p":"템플릿 상태 전환(활성/비활성/초안). UI에서는 '활성'/'비활성' 토글에 사용. 동일 상태로의 전환은 거부.","r":"void (returns void). 어댑터(setNotificationTemplateStatus)는 전환 후 loadNotificationTemplate(p_id)로 재조회한 MessageTemplate | null을 반환."},{"n":"admin_save_notification_group","op":"rpc","type":"supabase_rpc","p":"발송 대상 그룹(정적 명단/조건 기반) insert/update 겸용 저장. p_id=null이면 신규. 쓰기 단일 경로.","r":"uuid — 저장된 notification_groups.id 단일 스칼라. 어댑터(saveNotificationGroup)는 이 id로 loadNotificationGroup 재조회해 MessageGroup { id, name, description, definitionType('정적 그룹'|'조건 기반 그룹'), builderMode, channels:('mail'|'push'|'in_app')[], memberCount, "},{"n":"admin_delete_notification_group","op":"rpc","type":"supabase_rpc","p":"발송 대상 그룹 삭제. 어댑터는 삭제 직전 행을 읽어 두고 삭제 후 스냅샷을 반환.","r":"void (returns void). 어댑터(deleteNotificationGroup)는 삭제 전 읽어둔 MessageGroup | null을 반환."},{"n":"notification_templates","op":"select","type":"supabase_table","p":"알림 템플릿 원본 테이블. channel별 변형 행(unique(template_key, channel)). 프런트는 채널별 목록/단건 조회에 직접 SELECT(읽기)하며, 쓰기는 전부 admin RPC 단일 경로(직접 INSERT/UPDATE/DELETE는 RLS로 차단).","r":"SELECT 컬럼(TEMPLATE_COLUMNS): { id:uuid, template_key:text, channel:text, class:text, mandatory:boolean, mode:text, category:text, name:text, summary:text, subject:text, body_html:text, body_json:jsonb, variables:jsonb, trigger_key:text|null"},{"n":"notification_groups","op":"select","type":"supabase_table","p":"발송 대상 그룹(정적 명단/조건 기반) 테이블. 프런트가 목록/단건 직접 SELECT(읽기). 쓰기는 admin RPC 단일 경로.","r":"SELECT 컬럼(GROUP_COLUMNS): { id:uuid, name:text, description:text, definition_type:text('static'|'query'), builder_mode:text('simple'|'query-builder'), channels:jsonb, member_count:integer, rule_summary:text, filters:jsonb, query_config:json"},{"n":"notification_dispatches","op":"select","type":"supabase_table","p":"발송 실행 ledger(관리자 발송 이력 화면의 SoT 1계층). 관리자 1회 발송/스케줄러 1슬롯당 1행. 프런트는 최근 200건 목록을 직접 SELECT(읽기). 행 생성은 admin_send_notification RPC만.","r":"SELECT 컬럼(DISPATCH_COLUMNS): { id:uuid, template_id:uuid|null, template_key:text, channels:jsonb, target_type:text, target_group_ids:jsonb, recipient_count:integer, status:text, actor_id:uuid|null, reason:text|null, scheduled_at:timestamptz"},{"n":"notification_delivery_attempts","op":"select","type":"supabase_table","p":"수신자×채널 전달 결과(SoT 2계층). 관리자 상세 drawer가 dispatch별 시도 목록을 SELECT(읽기). 이메일 워커는 pending 행을 SELECT 후 status/provider_message_id/error/sent_at을 UPDATE(전달 결과 기록). v13 X-09 발송 이력 패널은 본인 행만 읽는 공유 객체.","r":"admin SELECT 컬럼(ATTEMPT_COLUMNS): { id:uuid, dispatch_id:uuid, user_id:uuid, channel:text, template_key:text|null, status:text, error_code:text|null, error_message:text|null, retry_count:integer, sent_at:timestamptz|null, created_at:timesta"},{"n":"/api/notifications/dispatch-email","op":"POST","type":"external_service","p":"이메일 발송 워커(서버리스 핸들러). notification_delivery_attempts에서 channel='email'·status='pending' 행을 배치(최대 50)로 가져와 SMTP로 실제 메일을 전송하고 결과를 기록한다. GET=cron 트리거(Authorization Bearer CRON_SECRET), POST=수동 트리거(x-worker-secret 헤더). 파일: api/notifications/dispatch-email.ts.","r":"성공: 200 { ok: true, processed: number, sent: number, failed: number }. 인증 실패: 401 { ok: false, error: 'unauthorized' }. SMTP 미구성: 503 { ok: false, error: 'smtp_not_configured' }(attempt 미변경 no-op). 서버 설정 누락: 500 { ok: false, error: 'server_"}]},{"domain":"writing (작성/외부 AI 채점/피드백)","project":"v13","apis":[{"n":"create_external_writing_submission","op":"rpc","type":"supabase_rpc","p":"외부 채점 API(TALKPIK)로 큐잉된 쓰기 제출에 대해 로컬 writing_submissions 행을 analyzing/failed 상태로 생성한다. 외부 제출이 정상 큐잉되면 analyzing, 외부 호출이 복구 가능 오류(네트워크/5xx)로 실패하면 failed로 로컬에만 기록한다. draft 단위 멱등(이미 활성 제출이 있으면 새 행 없이 기존 id 반환).","r":"string (uuid) — 생성/기존 writing_submissions.id. 멱등 경로에서는 이번 호출의 external_submission_id와 다른 기존 id를 반환할 수 있으므로 반환값을 신뢰해야 함. 실패 생성 경로는 createFailedLocalSubmission에서 data가 자신이 넘긴 submissionId와 같아야 함을 검증."},{"n":"sync_external_writing_feedback","op":"rpc","type":"supabase_rpc","p":"외부 채점 API의 평가 상태/결과를 로컬 피드백 테이블(writing_feedback, feedback_dimension_scores, sentence_feedback)과 writing_submissions.feedback_status로 동기화한다. /api/writing/evaluation-status 라우트가 폴링 중 외부 상태에 따라 호출.","r":"string (text) — 적용된 상태 문자열. complete가 아니면 입력한 next_status를 그대로 반환, complete면 'complete' 반환. (라우트는 반환값을 직접 쓰지 않고 syncError 유무만 확인.)"},{"n":"writing_drafts","op":"select/insert/update","type":"supabase_table","p":"자동저장되는 가변 임시 답안(draft). (user_id, problem_id) 당 superseded가 아닌 활성 draft 1개를 partial unique index로 보장. 작성 화면이 autosave로 upsert, 제출 시 RPC가 superseded 처리.","r":"Tables<'writing_drafts'> 행. { id: uuid; user_id: uuid; problem_id: uuid; question_no: 51|52|53|54; answer_text: string|null; answer_json: Json|null; char_count: number|null; autosave_status: 'clean'|'dirty'|'syncing'|'failed'|'superseded'; "},{"n":"writing_submissions","op":"select","type":"supabase_table","p":"제출된 불변 답안. 프론트는 SELECT만(INSERT/상태전이는 service-role RPC 경로). 비교 리포트·피드백 페이지·평가상태 라우트가 단건 조회.","r":"Tables<'writing_submissions'> 행. { id: uuid; user_id: uuid; problem_id: uuid; draft_id: uuid|null; question_no: 51|52|53|54; answer_text: string; answer_json: Json|null; char_count: number; submitted_at: string; feedback_status: 'pending'|'"},{"n":"writing_feedback","op":"select","type":"supabase_table","p":"제출 1:1 AI 종합 피드백. 프론트 읽기 전용(쓰기는 sync RPC=service_role). 피드백 번들/비교 리포트 메트릭 입력으로 단건 조회.","r":"Tables<'writing_feedback'> 행. { submission_id: uuid (PK); user_id: uuid; status: 'partial'|'complete'|'failed'; score_total: number|null; score_max: number|null; overall_summary: string|null; ai_model: string|null; ai_model_version: string|"},{"n":"feedback_dimension_scores","op":"select","type":"supabase_table","p":"정규화된 차원별 점수(문법/어휘/구조/내용/표현/주제적합). 프론트 읽기 전용. 피드백 번들·비교 메트릭 입력으로 다건 조회.","r":"Tables<'feedback_dimension_scores'>[] 배열. 각 행 { id: uuid; submission_id: uuid; user_id: uuid; dimension: 'grammar'|'vocab'|'structure'|'content'|'expression'|'topic_fit'; score: number|null; score_max: number|null; summary: string|null; wea"},{"n":"sentence_feedback","op":"select","type":"supabase_table","p":"문장 단위 교정(장문 피드백 E-02). 프론트 읽기 전용. 피드백 번들에서 sentence_index 오름차순 다건 조회.","r":"Tables<'sentence_feedback'>[] 배열. 각 행 { id: uuid; submission_id: uuid; user_id: uuid; sentence_index: number; original_text: string|null; corrected_text: string|null; comment: string|null }"},{"n":"GET /api/writing/evaluation-status","op":"GET","type":"next_api_route","p":"제출의 채점 상태를 폴링하는 Next 라우트 핸들러. 외부 TALKPIK API에서 상태/피드백을 가져와 sync RPC로 로컬에 반영하고 최신 feedback_status를 돌려준다. useFeedbackStatus가 5초 간격 최대 12회 폴링.","r":"200: { feedback_status: 'pending'|'analyzing'|'complete'|'failed' | null } ; 에러: { error: string } (400 submissionId required / 401 unauthorized / 403 account_inactive / 404 not found / 500 db error). 프론트(queries.ts fetchFeedbackStatus)는 bo"},{"n":"POST {TALKPIK_BASE}/api/writing/submit (external grader submit)","op":"POST","type":"external_service","p":"외부 TALKPIK 채점 API에 답안을 제출(큐잉)한다. submitWritingAction이 로컬 submission 생성 전에 호출하여 external submission_id를 확보.","r":"ExternalSubmitWritingResponse: { submission_id: string; status: string; message: string }. status==='failed'면 로컬 nextStatus='failed', 그 외엔 'analyzing'. submission_id를 create_external_writing_submission의 external_submission_id로 사용."},{"n":"GET {TALKPIK_BASE}/api/evaluation/{submissionId} (external status)","op":"GET","type":"external_service","p":"외부 TALKPIK API에서 제출의 현재 채점 상태를 조회. evaluation-status 라우트가 폴링 시 호출.","r":"ExternalEvaluationStatus: { submission_id: string; status: 'processing'|'graded'|'failed'|string; total_score?: number|null; max_score?: number|null; processing_time_seconds?: number|null }. status 매핑 → graded:피드백 fetch 후 complete, processi"},{"n":"GET {TALKPIK_BASE}/api/evaluation/{submissionId}/feedback (external feedback)","op":"GET","type":"external_service","p":"외부 채점 완료(graded) 시 상세 피드백(점수/차원/주석)을 가져온다. mapExternalEvaluationFeedback로 로컬 페이로드로 변환 후 sync RPC에 전달.","r":"ExternalEvaluationFeedback: { submission_id: string; status: string; total_score: number; max_score: number; processing_time_seconds: number; time_spent?: number|null; time_spent_seconds?: number|null; trait_scores: Array<{trait?|name?, sco"},{"n":"POST {TALKPIK_BASE}/api/writing/save-draft (external draft, STUB/미배선)","op":"POST","type":"external_service","p":"외부 TALKPIK API에 임시저장(draft)을 동기화하는 클라이언트 함수. 현재 도메인 코드 내 호출부 없음(정의만 존재) — 향후 외부 autosave 연동용 placeholder.","r":"ExternalSaveDraftResponse: { submission_id: string; saved_at?: string; character_count?: number }"}]},{"domain":"problems + learning (문제풀이/학습이벤트)","project":"v13","apis":[{"n":"list_user_problems","op":"rpc","type":"supabase_rpc","p":"C-02 문제 목록 화면용. 인증된 사용자(auth.uid())의 시점에서 published 문제 목록을 필터/정렬/페이지네이션하고, 각 문제에 사용자별 풀이 상태(객관식 attempt + writing draft/submission)를 결합해 반환한다. 클라이언트 후처리 필터의 페이지 카운트 오류를 없애기 위해 SQL window total_count로 정확한 전체 건수를 같이 내려준다.","r":"returns table — 0..N rows. 각 row(SQL 컬럼명): { problem_id: uuid, title: text, domain: text, topik_level: smallint|null, question_no: smallint|null, difficulty: smallint|null, tags: text[]|null, attempt_count: int (writing이면 submission+draft 보"},{"n":"problems","op":"select","type":"supabase_table","p":"문제 카탈로그(AI 생성 + 큐레이션 통합). 문제풀이 도메인은 published 문제를 목록/추천/다음문제 후보로 읽기만 한다(쓰기는 admin/작성자 경로, 이 도메인 프론트에서는 select 전용).","r":"select 컬럼 집합은 호출처마다 다름. (A) queries.ts fetchProblemList: { id, domain, question_no, topik_level, difficulty, title, publish_status, review_status, tags, updated_at } + { count:'exact' }. (B) next.ts pickProblemExcluding/fetchPublishedProble"},{"n":"problem_attempts","op":"select","type":"supabase_table","p":"객관식(reading/listening) 시도 기록. 이 도메인 프론트(문제풀이/학습)에서는 '이미 시도한 문제 제외'와 '최근 시도 question_no' 신호용으로 select만 한다. (writing 흐름은 problem_attempts가 아니라 writing_submissions/writing_drafts 사용 — 이 도메인 프론트에서 problem_attempts INSERT/UPDATE 호출은 발견되지 않음. list_user_problems RPC 내부 lateral join으로도 읽힘.)","r":"next.ts select 결과 row: { problem_id: uuid, started_at: timestamptz, problems: { id: uuid, question_no: number|null } | { ... }[] | null }. 테이블 전체 컬럼: { id uuid, user_id uuid, problem_id uuid, selected_answer jsonb|null, is_correct boolean|n"},{"n":"study_events","op":"insert","type":"supabase_table","p":"사용자별 학습 이벤트 시계열 원장. 분석/대시보드용. 프론트는 fire-and-forget으로 INSERT만 한다(읽기는 admin org dashboard RPC 등 다른 경로). PII 계약: payload는 primitive(string|number|boolean|null)만 허용하고 raw 글쓰기 내용 키(answer/content/draft/narrative 등)는 클라이언트에서 차단/삭제 후 insert. review_set 필터(list_user_problems)에서는 event_type='review_set_created' row를 SELECT로도 사용.","r":"INSERT는 반환값을 사용하지 않음(.insert(row), error만 확인). fire-and-forget: error 시 console.warn만 하고 Promise는 reject하지 않음. 테이블 row 컬럼: { id uuid pk, user_id uuid, event_type text, occurred_at timestamptz default now(), problem_id uuid|null, submission_"},{"n":"learning_goals","op":"upsert","type":"supabase_table","p":"사용자당 1개 활성 TOPIK 학습 목표(A-03/X-05). user_id PK 1:1. 프론트는 select(목표 조회) + upsert(저장)만. 대시보드/성장 페이지의 목표 달성률 계산(goalProgress) 입력으로도 사용.","r":"upsert(...).select('*').single() → 전체 row 반환: { user_id: uuid, topik_level: 'TOPIK_I'|'TOPIK_II', target_grade: number(1~6), exam_date: string|null (date), weekly_goal_minutes: number|null, weak_areas: string[], is_active: boolean, updated_"},{"n":"src/lib/practice/next.ts (getNextProblem / getNextProblemBundle)","op":"select","type":"next_api_route","p":"다음 문제 추천(R-02) 서버 전용 헬퍼. 별도 HTTP 라우트가 아니라 RSC/서버 컴포넌트(dashboard, practice/next page)에서 직접 호출하는 server client(RLS-bound, service role 아님) 조합 함수. 4-tier 폴백으로 primary 1문제 + summary 신호 + alternatives 3문제 번들을 만든다. 외부 LLM 호출 없음(기존 테이블 신호만 사용).","r":"NextProblemBundle = { primary: NextProblemSuggestion|null, primaryTier: 1|2|3|4, summary: { recentSubmissions: number, averageScore: number|null, weakestDimensions: {dimension:string,score:number}[] }, alternatives: AlternativeProblem[] }. "},{"n":"src/lib/events/study-events.ts (logStudyEvent + PII guard)","op":"insert","type":"next_api_route","p":"study_events INSERT 클라이언트 래퍼. 별도 HTTP 라우트 아님(브라우저 client 직접 insert). PII 계약 강제 계층: assertSafePayload(forbidden 키/200자 초과/네스티드 거부)+sanitizePayloadForInsert(prod에서 실제 strip). 백엔드 관점에서 study_events row contract와 동일.","r":"Promise<void> — 반환값 없음. 계약: 절대 reject하지 않음(dev/test의 'study-events:' assertion만 re-throw). 세션 없으면 silently no-op. 내부적으로 TablesInsert<'study_events'> row를 만들어 insert(상위 study_events 테이블 항목의 INSERT shape와 동일)."},{"n":"src/lib/growth/goalProgress.ts (calculateGoalProgress)","op":"GET","type":"next_api_route","p":"목표 달성률 순수 계산 함수(네트워크/DB 호출 없음 — 외부 API 아님). learning_goals(topik_level,target_grade) + writing_feedback(score_total,score_max) 입력을 받아 0~100 달성률(%)을 산출. dashboard/growth 페이지에서 호출. 백엔드는 이 함수에 들어가는 두 테이블의 필드 정합성만 보장하면 됨(STUB/외부엔드포인트 아님, 클라이언트 계산).","r":"number|null. null = 목표 없음/TOPIK_II 아님/targetGrade 없음/피드백 점수 없음. 그 외 Math.min(100, round((평균정규화점수 / 목표점수) * 100)). 보조 export: getTopikWritingTargetScore(level,grade)→number|null (TOPIK_II 총점 cutoff/3섹션: grade 3=120,4=150,5=190,6=230 → /3); n"}]},{"domain":"dashboard + recommendations (대시보드/추천/리포트)","project":"v13","apis":[{"n":"get_dashboard_kpi","op":"rpc","type":"supabase_rpc","p":"대시보드 상단 KPI 4종(오늘 응시수·누적 응시수·시험 D-day·연속 학습일)을 1회 왕복으로 반환. Phase 6에서 기존 4개 쿼리를 단일 RPC로 통합. KST(Asia/Seoul) 기준 하루 경계 계산을 SQL 내부에서 수행.","r":"// RETURNS TABLE — supabase-js는 단일행 setof를 Array<{...}>로 받음(프론트는 Array.isArray ? data[0] : data 로 1행 추출)\n{\n  today_attempts: number,   // int, problem_attempts에서 오늘(KST) started_at 카운트\n  total_attempts: number,   // int, problem_attempts 전체"},{"n":"recommendation_items","op":"select","type":"supabase_table","p":"사용자별 추천 문제 아이템(rank·이유·예상시간·취약태그). 대시보드/추천 페이지/약점 페이지/다음문제 추천의 핵심 소스. problems 테이블과 !inner 조인으로 제목·유형 노출.","r":"// select(\"id, problem_id, rank, reason, estimated_minutes, weakness_tags, problems!inner(title, question_no, publish_status, domain, difficulty)\")\n{\n  id: string,                    // recommendation_items.id (uuid) — consume 시 사용\n  proble"},{"n":"recommendation_runs","op":"select","type":"supabase_table","p":"추천 생성 이벤트(run) 레코드. run 단위 '왜 추천했나' 요약(reason_summary)·source_type·만료시각(expires_at) 제공. 한 run이 여러 recommendation_items를 낳음.","r":"// recommendations-data.ts select(\"id, source_type, reason_summary, created_at, expires_at\")\n{\n  id: string,            // uuid\n  source_type: string,   // 'dashboard'|'feedback'|'weakness'|'next_problem'\n  reason_summary: string | null,\n  "},{"n":"comparison_reports","op":"select","type":"supabase_table","p":"R-01 답안 비교 리포트 스냅샷(점수/차원 델타 metrics + AI 내러티브). 라이브러리 '리포트' 탭에서 generated_at·narrative 발췌 노출, 단건 조회로 상세 표시. AI 내러티브는 재현성 위해 보존.","r":"// 라이브러리: select(\"id, generated_at, narrative\")\n// 단건(writing/server.ts): select(\"*\") → ComparisonReportRow 전체\n{\n  id: string,                     // uuid\n  user_id: string,                // uuid\n  current_submission_id: string,  // uuid ("},{"n":"library_items","op":"select","type":"supabase_table","p":"F-01 '내 서재' 다형성 저장 원장. item_type별로 submission/report/problem/export(/attempt) 중 정확히 하나를 가리킴. 4개 탭 목록 조회 + 저장/태그수정/삭제.","r":"// select(\"*\") → LibraryItemRow(Tables<'library_items'>)\n{\n  id: string,             // uuid (item_id, 삭제/태그수정 키)\n  user_id: string,\n  item_type: 'attempt'|'submission'|'report'|'export'|'problem',\n  attempt_id: string | null,\n  submission_"},{"n":"library_items","op":"insert","type":"supabase_table","p":"서재에 항목 저장(submission/report/problem/export). 정확히 한 개의 *_id를 item_type에 맞춰 설정.","r":".insert(input).select('*').single() → LibraryItemRow 전체(위 select shape와 동일)"},{"n":"library_items","op":"update","type":"supabase_table","p":"서재 항목 태그 수정.","r":".update({tags}).eq('id',itemId).select('*').single() → LibraryItemRow"},{"n":"library_items","op":"delete","type":"supabase_table","p":"서재 항목 삭제.","r":"void(.delete().eq('id',itemId), 반환 데이터 없음)"},{"n":"avatars (storage bucket)","op":"upsert","type":"supabase_storage","p":"X-05 프로필 아바타 업로드. avatars/{userId}/avatar-<ts>.<ext> 경로로 업로드(upsert) 후 public URL 도출, profiles.avatar_path에 경로 저장. 실제 Supabase Storage 연동(스텁 아님).","r":"// upload → { error } / getPublicUrl(path) → { data: { publicUrl: string } }\n// uploadAvatar 반환: AvatarUploadResult { path: string; publicUrl: string }\n// 이후 profiles.update({avatar_path: path}).eq('id', userId)"}]},{"domain":"auth + account + affiliation (로그인/회원/탈퇴/기관코드)","project":"v13","apis":[{"n":"request_account_deletion","op":"rpc","type":"supabase_rpc","p":"로그인한 본인 계정을 셀프서비스로 소프트 삭제(회원 탈퇴)한다. 호출자 본인 profiles.status='deleted', deleted_at=now()로 멱등 전환. 복구(deleted->active)와 하드삭제는 범위 밖.","r":"void (반환값 없음 / Returns: undefined). 성공 시 data=null, error=null. 실패 시 PostgrestError(error.message/code). 프론트는 error 유무만 검사한다."},{"n":"is_nickname_available","op":"rpc","type":"supabase_rpc","p":"닉네임 후보가 사용 가능한지(다른 회원이 쓰지 않는지) 다른 profiles 행을 노출하지 않고 확인. 프로필 편집 화면의 닉네임 실시간 중복검사.","r":"boolean — 사용 가능하면 true, 불가/빈값이면 false. 프론트는 data === true로 비교(checkNicknameAvailability)."},{"n":"claim_affiliation_code","op":"rpc","type":"supabase_rpc","p":"OAuth 가입 등 가입 메타데이터로 affiliation_code를 못 받은 경로에서, 로그인 직후 클라이언트가 localStorage에 보관해둔 기관(박람회 QR) 코드를 본인 profiles.affiliation_code에 1회 백필. 이미 설정돼 있으면 no-op.","r":"text | null — 적용된(검증 통과한) 코드 문자열을 그대로 반환, 형식 불일치면 null. 프론트(affiliation-code.ts)는 반환값을 쓰지 않고 error 유무만으로 'claimed'|'failed'|'empty' 결과를 만든다."},{"n":"profiles","op":"select","type":"supabase_table","p":"auth.users와 1:1 미러. 신뢰 속성(app_role/plan_label/status)과 사용자 편집 속성(display_name/nickname/ui_locale/nationality_country_code/bio/notification_prefs/avatar_path) + 가입 출처 affiliation_code/탈퇴시각 deleted_at. INSERT는 클라가 못함(on_auth_user_created 트리거가 생성). 본인 행만 select/update(보호컬럼 제외).","r":"Tables<'profiles'> Row: { id: uuid; display_name: string|null; nickname: string|null (citext); avatar_path: string|null; ui_locale: 'ko'|'en'|'vi'; app_role: 'learner'|'content_admin'|'org_admin'|'platform_admin'; plan_label: string; status"},{"n":"user_consents","op":"insert","type":"supabase_table","p":"약관/개인정보(legal_documents) 동의 이력 추가전용 원장. 어떤 문서 버전을 언제, 어떤 출처(signup/re_consent/settings)로 수락했는지 1행씩 기록. 가입/재동의 플로우에서 사용. 본인 행 read + insert만, update/delete 없음(불변).","r":"TablesInsert<'user_consents'> 입력: { user_id: uuid; document_id: uuid; doc_type: 'terms'|'privacy'; version: string; source: 'signup'|'re_consent'|'settings' (default 'signup') }. select 시 Row 추가 필드: { id: uuid; accepted_at: timestamptz; cre"},{"n":"user_marketing_consent","op":"select","type":"supabase_table","p":"마케팅 알림 EXPLICIT opt-in 동의/수신거부 저장(H-2). 유효 동의 = consented_at IS NOT NULL AND unsubscribed_at IS NULL. unsubscribe_token(uuid)은 세션 없는 이메일 수신거부 링크의 인증 수단. profiles 미변경 가산형 테이블.","r":"Row: { user_id: uuid (PK, profiles FK ON DELETE CASCADE); consented_at: timestamptz|null; unsubscribed_at: timestamptz|null; unsubscribe_token: uuid (NOT NULL, default gen_random_uuid(), UNIQUE); source: text|null; updated_at: timestamptz }"},{"n":"GET /auth/callback","op":"GET","type":"next_api_route","p":"이메일 인증/비밀번호 재설정/OAuth 콜백 처리 Route Handler. token_hash+type(PKCE), code(OAuth code), provider error, 또는 fragment(implicit) 분기 처리 후 세션 쿠키를 Set-Cookie로 emit하고 next(또는 /auth/error)로 303 redirect.","r":"HTTP 302/307 Redirect (NextResponse.redirect). 본문 JSON 아님. 성공: Location=next(0.0.0.0->localhost 보정), Set-Cookie=세션쿠키. 실패: Location=/auth/error?reason=<mapped>[&retry_after_seconds=<n>]. 토큰 없음(implicit): Location=/auth/callback-fragment?next"},{"n":"POST /auth/sign-out","op":"POST","type":"next_api_route","p":"서버사이드 로그아웃. supabase.auth.signOut()으로 세션 쿠키 무효화 후 /login으로 303 redirect. POST 전용(CSRF 보호 — GET 링크/img 태그로 로그아웃 트리거 차단).","r":"POST: HTTP 303 Redirect → Location=/login (next 쿼리 있어도 무시, 항상 /login). 본문 없음. GET: 405 JSON { error: 'Method Not Allowed', allow: ['POST'] } + Allow: POST 헤더."},{"n":"POST /auth/account-delete","op":"POST","type":"next_api_route","p":"회원 탈퇴 실행 Route Handler. 설정 danger-zone 모달 폼 submit. getUser로 본인 확인 → request_account_deletion RPC(소프트삭제) → signOut({scope:'global'})(전기기 refresh token 폐기) → /login?reason=withdrawn 303.","r":"POST: 성공 303 → /login?reason=withdrawn (전역 signOut). 미인증 303 → /login. RPC 실패 303 → /settings/account?delete=error (세션 유지, danger-zone이 인라인 에러 표시). GET: 405 JSON { error:'Method Not Allowed', allow:['POST'] } + Allow:POST."},{"n":"GET /auth/account-inactive","op":"GET","type":"next_api_route","p":"탈퇴(deleted)/차단(blocked) 계정이 아직 만료 안 된 access token을 들고 있을 때 로컬 세션 쿠키를 정리하는 Route Handler. workspace layout이 비활성 status 감지 후 여기로 redirect(Server Component는 쿠키 못 지움). signOut({scope:'local'}) 후 /login?reason= 으로 303.","r":"HTTP 303 Redirect → /login?reason=withdrawn (status!=blocked) | /login?reason=blocked (status=blocked). 본문 없음. 로컬 세션 쿠키 정리."}]},{"domain":"notifications (인앱/이메일/구독해지)","project":"v13","apis":[{"n":"user_notifications","op":"select","type":"supabase_table","p":"인앱 알림 함(벨 뱃지 / 팝오버 인박스 / B-01 대시보드 알림 카드)의 표시 데이터. 로그인 사용자 본인 알림만 조회.","r":"UserNotification = { id: string; template_key: string; category: 'study'|'exam_schedule'|'notice'|'event'|'marketing'; title: string; body: string; link_url: string | null; read_at: string | null; created_at: string }  // 미읽음 카운트 쿼리는 { coun"},{"n":"user_notifications (read_at update)","op":"update","type":"supabase_table","p":"알림 읽음 처리. 단건(항목 클릭) 및 전체 읽음(markAllRead). read_at 컬럼만 갱신 가능.","r":"{ error: { message: string } | null }  // 반환 행 없음(void). 프론트는 낙관적 업데이트 후 에러 시 롤백"},{"n":"notification_settings","op":"select","type":"supabase_table","p":"X-09 알림 설정 화면 로드. 사용자별 리마인더 시각/요일/채널/타임존(인앱·이메일·Zalo 토글의 영속 상태).","r":"NotificationSettings = { reminder_time: string | null /* HH:mm:ss */; reminder_days: number[] /* 0..6 */; channels: { in_app: boolean; email: boolean; zalo: boolean }; timezone: string }  // 행 없으면 기본 { reminder_time:null, reminder_days:[], "},{"n":"notification_settings (upsert)","op":"upsert","type":"supabase_table","p":"X-09 알림 설정 저장. 리마인더 시각/요일/채널/타임존을 user_id 충돌 기준 upsert.","r":"{ error: { message: string } | null }  // 반환 행 없음(void). onConflict: 'user_id'"},{"n":"notification_log","op":"select","type":"supabase_table","p":"[DEPRECATED] X-09 발송 이력 패널. 현재는 notification_delivery_attempts로 대체됨(fetchDeliveryHistory). notification_log 테이블 폐기 전까지 코드만 잔존.","r":"NotificationLogEntry = { id: string; channel: string; template_key: string; status: 'sent'|'failed'|'pending'; sent_at: string | null; created_at: string }"},{"n":"notification_delivery_attempts","op":"select","type":"supabase_table","p":"X-09 채널별 발송 이력(현행). 사용자별 최근 전송 시도 N건을 읽기 전용으로 표시.","r":"DeliveryHistoryEntry = { id: string; channel: string; template_key: string; status: 'sent'|'failed'|'pending'|'skipped'|'opted_out'|'deduped'; sent_at: string | null; created_at: string }"},{"n":"notification_delivery_attempts (worker update)","op":"update","type":"supabase_table","p":"이메일 워커가 pending email attempt를 transport 결과로 종결(sent/failed/pending 재시도). dispatch-email 라우트 내부 service-role 사용.","r":"// SELECT 단계: { data: Array<{ id, user_id, dispatch_id, template_key, retry_count }>, error } — channel='email' AND status='pending' ORDER BY created_at ASC LIMIT 50.  UPDATE 단계: { error: { message } | null }"},{"n":"notification_dispatches","op":"select","type":"supabase_table","p":"이메일 워커가 attempt의 dispatch_id → template_id를 해석하기 위해 조회(템플릿 본문 찾기 1차 경로). service-role.","r":"{ template_id: string | null }  // maybeSingle"},{"n":"notification_templates","op":"select","type":"supabase_table","p":"이메일 워커가 발송할 제목/본문/링크/class를 조회. dispatch.template_id 경유(1차) 또는 template_key+channel='email'+status='active' 폴백(2차).","r":"{ subject: string | null; body_html: string | null; link_url: string | null; class: string | null }  // maybeSingle. 워커가 ResolvedContent로 매핑(+ profiles.display_name 추가)"},{"n":"user_marketing_consent (worker select)","op":"select","type":"supabase_table","p":"이메일 워커가 marketing class 메일에 붙일 수신거부 토큰을 해석. service-role.","r":"{ unsubscribe_token: string | null }  // maybeSingle"},{"n":"user_marketing_consent (unsubscribe select+update)","op":"update","type":"supabase_table","p":"마케팅 수신거부 처리. 이메일 링크 클릭(세션 없음) → 토큰으로만 조회 후 unsubscribed_at 기록. 멱등.","r":"// 라우트 응답(테이블 행 아님): GET→HTML 페이지(200 완료/이미됨, 400 잘못된토큰, 500 오류).  POST→ { ok:true, status:'수신거부 처리되었습니다.'|'이미 수신거부됨.' } 또는 { ok:false, error:'invalid_token'|'server_misconfigured'|'unsubscribe_failed' }"},{"n":"POST /api/notifications/dispatch-email","op":"POST","type":"next_api_route","p":"앱-사이드 이메일 워커. pg_net 미설치로 SQL이 발송 못 하는 live 모드의 pending email attempt를 nodemailer(Daou Office SMTP)로 실제 발송하고 결과를 기록. cron이 호출.","r":"성공: { ok: true, processed: number, sent: number, failed: number } (200)  |  실패: { ok: false, error: 'unauthorized' }(401) | { ok:false, error:'smtp_not_configured' }(503) | { ok:false, error:'server_misconfigured' }(500) | { ok:false, error"},{"n":"GET /api/notifications/unsubscribe","op":"GET","type":"next_api_route","p":"마케팅 이메일 본문의 수신거부 링크 클릭(브라우저 네비게이션) 처리. 토큰으로만 인증, HTML 확인 페이지 반환. 멱등.","r":"HTML 페이지(Content-Type text/html): 200 '수신거부 완료'(신규 처리/이미됨) | 400 '잘못된 요청'(유효하지 않은 링크) | 500 '일시적 오류'(misconfigured/error)"},{"n":"POST /api/notifications/unsubscribe","op":"POST","type":"next_api_route","p":"프로그램적/폼 제출 방식 수신거부. GET과 동일 로직, JSON 응답.","r":"성공: { ok: true, status: string }(200)  |  실패: { ok:false, error:'invalid_token' }(400) | { ok:false, error:'server_misconfigured' }(500) | { ok:false, error:'unsubscribe_failed' }(500)"},{"n":"private.dispatch_notifications / dispatch_scheduled_notifications / dispatch_admin_notifications / dispatch_notification_event / retry_failed_email_attempts / notification_email_transport / finalize_email_attempt / render_notification_text","op":"rpc","type":"supabase_rpc","p":"[프론트 비호출 — pg_cron 전용] 알림 발송 SQL 디스패처 일체. 스케줄형(study_reminder/weekly_summary) in_app·email, 관리자 발송, 이벤트형(feedback_ready), 이메일 재시도. 시각 출처=DB now() 단일.","r":"jsonb 집계 객체 (예: { at, study_reminder:{...}, weekly_summary:{...}, admin:{processed,dispatches}, email_retry:{retried,succeeded,still_failed,deferred} }). 프론트 타입 매핑 없음 — cron/관리자 백엔드 전용."}]},{"domain":"export (PDF 내보내기)","project":"v13","apis":[{"n":"POST /api/export/pdf","op":"POST","type":"next_api_route","p":"서버 측에서 react-pdf로 실제 PDF 파일을 동기 생성하는 메인 라우트(F-M1). 한 요청 안에서: ①세션/계정상태 확인 ②월 한도 검사 ③export_files row(status='queued') 삽입 ④대상 데이터(submission/report/library_selection) 조회+렌더 ⑤generated-exports 버킷 업로드 ⑥status='ready'+ready_at 갱신 ⑦study_events('export_downloaded') 텔레메트리. 실패 시 row를 status='failed'로 남기고 4xx/5xx JSON 반환 → 클라이언트는 브라우저 인쇄 폴백으로 전환.","r":"성공(200): { exportId: string(uuid); storagePath: string(=\"exports/{user_id}/{export_id}.pdf\"); filename: string(=\"{sanitized}.pdf\") }  // 클라이언트 타입 ServerPdfExportResult\n실패: { error: string } (한국어 사용자 메시지). 400=잘못된 JSON/옵션 검증 실패, 401=미로그인('로그"},{"n":"@react-pdf/renderer (renderToBuffer)","op":"POST","type":"external_service","p":"서버 라우트 내부에서 PDF 바이너리를 메모리 버퍼로 렌더링하는 외부 라이브러리. buildPdfDocument(@/lib/export/pdf-document)가 만든 React PDF 문서 트리를 renderToBuffer로 Buffer화한 뒤 generated-exports 버킷에 application/pdf로 업로드. registerPdfFonts()로 폰트 파일(fs) 사전 등록 필요(Edge 런타임 불가 사유).","r":"renderToBuffer(document) → Promise<Buffer> (PDF 바이너리). 이후 supabase.storage.from('generated-exports').upload(path, buffer, {contentType:'application/pdf', upsert:false})로 저장."},{"n":"export_files (insert — queued)","op":"insert","type":"supabase_table","p":"서버 PDF 생성 시작 시 ledger row 생성. status='queued', storage_path는 임시 placeholder(server-render://{uuid}), options에 {source:'server_render', ...요청옵션} 저장. 업로드 성공 후 별도 update로 실경로/ready 전환. 재시도 포함 모든 시도가 새 row(§3-H).","r":".insert({...}).select('id').single() → { id: string(uuid) }. insertError 또는 빈 결과면 500 '내보내기 기록을 만들지 못했어요.'"},{"n":"export_files (insert — browser_print, ready)","op":"insert","type":"supabase_table","p":"브라우저 인쇄(Tier1 MVP / 서버생성 실패 폴백) 경로에서 곧바로 status='ready' ledger row 삽입. storage_path='browser-print://{uuid}'(실제 파일 없음), options={source:'browser_print'}, ready_at=now. 큐 워커 없음(OOS-6). 이후 window.print() 호출.","r":".insert({...}).select('id').single() → { id: string(uuid) }. error throw, 빈 결과면 'triggerPdfExport: empty insert result'. 클라이언트 반환 PdfExportResult = { exportId: string }"},{"n":"export_files (update — ready / failed)","op":"update","type":"supabase_table","p":"서버 생성 라우트에서 PDF 업로드 성공 후 storage_path를 실경로로 갱신하고 status='ready'+ready_at 기록. 생성 실패(markFailed) 시 status='failed'로 전환(에러 swallow).","r":".update({...}).eq('id', exportId) → { error }. updateError면 throw 'export_files update: ...' → catch에서 markFailed + 500. markFailed는 .then(noop,noop)로 결과 무시."},{"n":"export_files (select — monthly limit count)","op":"select","type":"supabase_table","p":"월 PDF 내보내기 한도 검사. 이번 달(UTC 월초~익월초) 내 status≠'failed' row 수를 head count로 세어 PDF_EXPORT_MONTHLY_LIMIT(3) 이상이면 429 throw.","r":"{ count: number | null, error }. count≥3이면 PdfExportRequestError(429,'PDF 내보내기는 월 3회까지 사용할 수 있어요.'). error면 throw 'pdf export limit: ...' → 라우트가 500."},{"n":"export_files (select — library exports tab join)","op":"select","type":"supabase_table","p":"내 서재 '내보내기' 탭 렌더용. library_items(item_type='export')의 export_id로 export_files를 일괄 조회해 JS-side 조인. 상태 배지/소스 라벨/storage_path/다운로드 버튼 노출.","r":"rows → LibraryExportView[] (server.ts joinExports 매핑): { kind:'export'; id:string; source_type:'submission'|'report'|'library_selection'; storage_path:string; status:'queued'|'ready'|'failed'; options:Json|null; item_id:string(=library_item"},{"n":"generated-exports (Storage upload)","op":"POST","type":"supabase_storage","p":"서버 라우트가 렌더된 PDF Buffer를 비공개 버킷 generated-exports의 exports/{user_id}/{export_id}.pdf 경로에 업로드. contentType application/pdf, upsert=false(중복 방지). 실패 시 throw → row failed + 500.","r":"supabase.storage.from('generated-exports').upload(path, buffer, opts) → { data, error }. uploadError면 throw new Error(`storage upload: ${error.message}`)."},{"n":"generated-exports (Storage download)","op":"GET","type":"supabase_storage","p":"클라이언트가 서버 생성 응답의 storagePath로 PDF Blob을 내려받아 브라우저 다운로드(a[download] 트리거). 내 서재 다운로드 버튼도 동일 경로 사용.","r":"supabase.storage.from('generated-exports').download(path) → { data: Blob | null, error }. error||!data면 throw Error(error.message ?? 'download failed'). data Blob을 URL.createObjectURL로 다운로드."},{"n":"study_events (insert — export_downloaded)","op":"insert","type":"supabase_table","p":"PDF 내보내기 텔레메트리 ledger. 서버 생성 성공 시 + 브라우저 인쇄 시 각각 event_type='export_downloaded' row 1건 삽입. KPI 집계용. 실패는 swallow(.then(noop,noop))되어 내보내기/인쇄 흐름을 막지 않음. 버튼이 직접 로깅하지 않음(이중 집계 방지).","r":".insert({...}).then(noop, noop) — 반환값/에러 무시(fire-and-forget). 응답 미사용."},{"n":"writing_submissions / comparison_reports / problems (read for PDF items)","op":"select","type":"supabase_table","p":"서버 생성 시 PDF 본문 데이터를 모으는 읽기. submission 항목=getSubmission(+problems.title 조회 + includeFeedback면 getFeedbackBundle), report 항목=comparison_reports(id,narrative,generated_at). library_selection은 library_items(id,item_type,submission_id,report_id,saved_at)를 itemIds로 조회 후 각 항목 로드.","r":"PdfExportItem[]: submission → {kind:'submission', questionNo, problemTitle:string|null, submittedAt(YYYY-MM-DD), answerText, charCount, feedback:null|{scoreTotal,scoreMax,overallSummary,dimensions:[{dimension,score,scoreMax,summary}],senten"}]}]

const SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['items'],
  properties: {
    items: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['n', 'op', 'easy', 'easyResp'],
        properties: {
          n: { type: 'string' },
          op: { type: 'string' },
          easy: { type: 'string', description: 'plain Korean, <=45 chars, what this API DOES in everyday words (no jargon, no table/column names)' },
          easyResp: { type: 'string', description: 'plain Korean, 1 short sentence: what comes back / what the screen can show with it' },
        },
      },
    },
  },
}

phase('Rewrite')

const prompt = (d) => [
  '아래는 한 도메인의 API 목록(JSON)입니다. 도메인:', d.domain, '(프로젝트:', d.project + ').',
  '',
  '각 API를 개발 지식이 적은 사람(기획자/신입 백엔드)도 한눈에 이해할 수 있게 한국어로 쉽게 바꿔주세요. 규칙:',
  '- easy: 이 API가 "무엇을 하는지" 한 문장(45자 이내). 일상어로. 테이블명/컬럼명/RPC명 같은 전문용어 금지. 예) "관리자가 회원을 정지/해제한다", "쓰기 답안을 외부 채점에 보낸다".',
  '- easyResp: 응답으로 "무엇을 받는지/화면에 뭘 보여줄 수 있는지" 한 문장. 예) "성공 여부만 돌아온다", "회원 목록과 총 개수가 돌아온다", "점수와 문장별 첨삭이 돌아온다".',
  '- n, op 는 입력 그대로 되돌려 주세요(매칭용).',
  '- 추측하지 말고 주어진 purpose/response 내용에 근거해서 쉽게 풀어 쓰기만 하세요.',
  '',
  'API 목록(JSON):',
  JSON.stringify(d.apis),
  '',
  'items 배열만 구조화해서 반환하세요. 모든 API가 빠짐없이 포함되어야 합니다.',
].join('\n')

const results = await parallel(DATA.map((d) => () =>
  agent(prompt(d), { label: 'plain:' + d.project + ':' + d.domain.split(' ')[0], phase: 'Rewrite', schema: SCHEMA })
))

return { byDomain: DATA.map((d, i) => ({ domain: d.domain, project: d.project, items: (results[i] && results[i].items) || [] })) }
