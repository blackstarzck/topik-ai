# TOPIK AI Admin 페이지 흐름도

## 개요

- 공통 흐름은 `검색/필터 -> 상세/확인 -> 조치 -> 피드백 -> 감사 로그 확인`입니다.
- placeholder 모듈은 `메뉴 진입 -> 역할/예정 기능 확인 -> 상세 정의 대기` 흐름을 가집니다.

## Users > 회원 목록

```mermaid
flowchart LR
  A["회원 목록 진입"] --> B["검색/필터 적용"]
  B --> C["결과 테이블 확인"]
  C -->|"행 클릭"| D["회원 상세 이동"]
  C -->|"조치 실행"| E["확인 모달"]
  E -->|"확인"| F["상태 변경"]
  F --> G["감사 로그 확인"]
```

## Users > 강사 관리

```mermaid
flowchart LR
  A["강사 관리 진입"] --> B["검색/필터 적용"]
  B --> C["강사 목록 확인"]
  C -->|"행 클릭"| D["강사 상세 Drawer"]
  D --> E["담당 과정/메시지 이력 확인"]
  D --> I["대상 그룹 보기"]
  D -->|"정지/해제"| F["확인 모달 + 사유 입력"]
  F --> G["상태 변경"]
  G --> H["감사 로그 확인 (Instructor)"]
```

## Message > 메일 / 푸시

```mermaid
flowchart LR
  A["채널 진입"] --> B["자동/수동 탭 선택"]
  B --> C["목록 확인 또는 등록 버튼 클릭"]
  C -->|"템플릿 등록"| D["템플릿 등록 모달(메타 정보)"]
  D -->|"등록"| E["테이블 갱신"]
  E -->|"행 클릭 또는 등록 상세 이동"| F["등록 상세 페이지"]
  C -->|"템플릿 정보 수정"| G["템플릿 정보 수정 모달"]
  G --> E
  E -->|"미리보기/테스트"| I["미리보기/테스트"]
  F --> H["TinyMCE 본문 작성 및 저장"]
  H --> I
  I --> J["발송 실행"]
  J --> K["발송 이력 확인"]
  K --> L["감사 로그 확인"]
```

## Message > 대상 그룹

```mermaid
flowchart LR
  A["대상 그룹 진입"] --> B["목록 확인"]
  B --> C["Drawer 열기"]
  C --> D["세그먼트 조건 편집"]
  D --> E["조회하기로 예상 대상 수 계산"]
  E --> F["저장 또는 삭제"]
  F --> G["감사 로그 확인"]
```

## Message > 발송 이력

```mermaid
flowchart LR
  A["발송 이력 진입"] --> B["채널 탭 선택"]
  B --> C["전체/자동/수동 필터"]
  C --> D["목록 확인"]
  D --> E["상세 Drawer 열기"]
  E --> F["재시도"]
  F --> G["감사 로그 확인"]
```

## Assessment / Content Placeholder

```mermaid
flowchart LR
  A["메뉴 진입"] --> B["placeholder 화면 노출"]
  B --> C["owner role 확인"]
  C --> D["예정 기능 목록 확인"]
  D --> E["상세 요구사항 확정 대기"]
```
